Generated by workflow k3t2-rigidity-loop-v2; computations tier B at best; physical identifications tier L/C.

# K3 x T2 rigidity loop v2: report

- Branch `loop/k3t2-rigidity`, worktree `/mnt/disks/disk-socrateai-local-1/dualscale-wt-k3t2`. Nothing has been pushed.
- Sealed targets: LeanMaster v3.20.0 (head `b9ce4144`), in `/mnt/disks/disk-socrateai-local-1/k3t2-sealed-v2/targets_sealed_v2.json`, outside the worktree.
- Run: wf_ca8ac00f-275, resumed after the orchestrator crashed at 06:16 UTC on 2026-09-19.
- Machine-readable version: `/mnt/disks/disk-socrateai-local-1/dualscale-wt-k3t2/audit/k3t2_rigidity_v2/agreement_ledger_v2.json`, commit `9e34ce8`. It is built by `/mnt/disks/disk-socrateai-local-1/dualscale-wt-k3t2/audit/k3t2_rigidity_v2/build_ledger_v2.py`.
- The builder reads only committed files inside the worktree. It computes every count below from a per-row classification, and it is deterministic: two runs gave byte-identical output (`cmp`).
- `PY` = `/home/callensxavier_gmail_com/SocrateAI-Scientific-DualScaleSimulator/.venv-tda/bin/python`. `W` = `/mnt/disks/disk-socrateai-local-1/dualscale-wt-k3t2/audit/k3t2_rigidity_v2`.
- Tier B = exact arithmetic plus a negative control. Tier L = identification from the literature. Tier C = conjecture. Nothing in this report is claimed as a theorem.

## 1. Bottom line

**Coverage**
- The sealed file holds **312 statements** (311 distinct): the 271 track targets plus 41 extras (27 "elsewhere", 12 orientifold, 2 out of scope).
- `orientifold#0` duplicates `E-flux#8` (same file and line: `Flux/Tadpole.lean:116`).
- **Every statement has a row.** v1 rowed 12 of 32.

**Comparator verdicts:** 93 AGREE (70 full coverage, 23 partial), 7 NOT_COMPARABLE (all match under a conversion), 3 DISAGREE, 205 NOT_COMPUTED, 4 OUT_OF_SCOPE. The comparator counted 13 rows as having two or more routes.

**Classification rules after both skeptics** (the rules are in the builder's docstring)
- The blindness skeptic's two Göttsche rows (`B-dyons#6`, `#36`) become NOT_COMPUTED. Track B's part1 compares the Göttsche formula with itself.
- `D-tda#56` goes from NOT_COMPARABLE to AGREE (math skeptic).
- Every other row either skeptic disputed stays AGREE but is marked **qualified**, meaning its independence or route count was overstated. So is `B-dyons#5`, which a blindness violation names as citing that same tautological part1 data.
- One rule is mine. It applies the blindness skeptic's objection to C/02 (a typed Gram matrix) and to E's typed quotation arithmetic, to `elsewhere#11` and `#12`, which the skeptic missed.

**Counts after the skeptics**

| | All 312 rows | 271 track targets |
|---|---|---|
| AGREE, clean | **77** (76 distinct; 56 full coverage, 21 partial) | **62** (43 full, 19 partial) |
| AGREE, qualified | **15** (all full coverage) | **8** |
| NOT_COMPARABLE, matching under conversion | 6 | 4 |
| DISAGREE against the blind tracks | **0** | **0** |
| DISAGREE where the sealed Lean is wrong | **3** (orientifold#8, #9, #11) | 0 |
| NOT_COMPUTED | 207 | 197 |
| OUT_OF_SCOPE | 4 | 0 |
| Rows with 2 or more independent blind routes | **0** (comparator: 13) | 0 |

**Comparison with v1**
- v1: 32 sealed, 12 rowed, 5 clean / 3 qualified / 0 disagree.
- v2: 101 rows compared. **77 clean, 15 qualified, 0 disagree against the blind tracks.**
- v2's clean A/B rows still inherit the definitional normalisation Z_K3 = 2 phi_{0,1}, as v1's did.
- No row keeps two independent blind routes. Every "24 by two routes" claim collapses to GUDHI plus a normalisation-conditional elliptic genus (§4).

**The 3 DISAGREEs**
- All three are in the sealed `StringTheoryFoundation/StringTheory/TadpoleCancellation.lean` at lines 41, 52 and 68. Both skeptics find that the sealed values are wrong.
- The math skeptic re-derived the correct values independently from Kodaira fibre Euler numbers: 4 I_0* fibres x 6 = 24 = e(K3), while 16 O7 planes would need 96.
- LeanMaster's post-seal commit `adc85e7` (v3.21.0) moved exactly these three statements to the blind values.
- Process caveat: a post-seal change weakens the seal for those three rows.

**Rigidity:** the tracks made 14 rigidity entries. **4 of 14 are genuinely rigid after I settled the two points where the skeptics disagree, and 2 of 14 on which both skeptics agree** (B's N = 324 and M = 648). v1 had 3 of 11. The tracks themselves claimed 6 RIGID (§3).

**Reverse pass:** 3 predictions, all held, none failed. Only one is reachable in Lean now. Item 3's independence is overstated (§6).

## 2. Per-track table

Counts are after both skeptics, per group of sealed statements:

| Group | Rows | Clean | Qualified | NC (match) | Sealed-Lean error | Not computed | Out of scope |
|---|---|---|---|---|---|---|---|
| A-genus | 105 | 20 | 2 | 0 | 0 | 83 | 0 |
| B-dyons | 63 | 12 | 1 | 4 | 0 | 46 | 0 |
| C-lattices | 36 | 15 | 2 | 0 | 0 | 19 | 0 |
| D-tda | 58 | 11 | 2 | 0 | 0 | 45 | 0 |
| E-flux | 9 | 4 | 1 | 0 | 0 | 4 | 0 |
| elsewhere | 27 | 10 | 6 | 0 | 0 | 10 | 1 |
| orientifold | 12 | 5 | 1 | 2 | 3 | 0 | 1 |
| out_of_scope | 2 | 0 | 0 | 0 | 0 | 0 | 2 |

### Track A: elliptic genus and moonshine (`W/A-genus`, commit 521215f)

Command: `cd W/A-genus && PY run_track_a_v2.py`

What it computed:
- Z_K3 = 2 phi_{0,1} through q^10. Z(tau,0) = 24, and every q > 0 term is 0.
- c(D) for D up to 40. The dependence on D alone holds with zero mismatches.
- A_1..A_10 = 45, 231, 770, 2277, 5796, 13915, 30843, 65550, 132825, 260568.
- Twined H_g for 2A, 3A, 5A and 7A. Integrality and A_n^(g) = A_n mod ord(g) hold for n <= 8.

Status:
- 22 AGREE rows, including `hComputed_eq_table`, `twined_2A/3A/5A/7AB` and the lock and ratio rows.
- Qualified: `A-genus#60 ellipticGenus_z0` and `#63 decomposition_pins_chi`. The blind 24 is a typed 2 times a computed 12 (`run_track_a_v2.py:81,209`).
- The CDH chi(g)/F_g inputs were recalled from memory. The math skeptic rebuilt them structurally: frame shapes plus dim M_2(Gamma0(N)) = 1 plus 2 ground states (`skA_twining.py`). Raw coefficients agree for all four classes, and a t -> t+1/3 control fails.

### Track B: quarter-BPS dyons (`W/B-dyons`, commit 79951e8)

Commands:
- `cd W/B-dyons && PY theta_forms.py 40 && PY hurwitz_class_numbers.py && PY part1_euler_numbers.py && PY part2_psi_m.py && PY part3_polar.py`
- margin check: `cd W/B-dyons/margin_check_QMAX60 && PY theta_forms.py 60 && cd .. && PY margin_stability_check.py`

What it computed:
- chi = c(0) + 2c(-1) = 24.
- DMZ (5.16) at m = 1 and m = 2 through q^10.
- Immortal m = 1: (N, M) = (324, 648).
- A Hurwitz table for D <= 40. The math skeptic confirmed it by an independent Dirichlet/conductor route at all 21 tabulated D.

Status:
- 12 clean. Examples: `c_depends_only_on_D`, `c_first`, `dmz_516_q1/q2` (partial), `polar_coefficient_pinned`.
- 4 NOT_COMPARABLE, all matching:
  - `hurwitz_values` (h12 = 12H);
  - `immortal_m1` (54 x 12 = 648);
  - `dmz_911_table` and `dmz_912_table` (x12).
- Qualified: `p24_values`.
- Demoted to NOT_COMPUTED: `goettsche` and `twined_goettsche_identity`.
- The math skeptic upgrades `immortal_m1`: the E4*A coefficient 3 is fixed by part2 and by skB's exact solve (b = 1), so all three numbers the statement contains (3, 324, 54) match.

### Track C: lattices and T-duality (`W/C-lattices`, commit ee3b1bd)

Commands: `PY W/C-lattices/01_e8.py`, then `02_k3_mukai_gamma.py`, `03_symbolic_tduality_proofs.py`, `04_tadpole_budget.py`, `06_chained_rigidity.py` (run after D) and `merge_results.py`.

What it computed:
- E8: det 1, positive definite, 240 roots by Weyl closure.
- K3 lattice (3,19), Mukai lattice (4,20) and Gamma^{6,22} (6,22), each by two signature methods.
- The T-duality identities, for d = 1..4.
- A chained (m,n) = (3,2).

Status:
- 17 AGREE. Qualified: `sigK3_eq` and `rank_K3`, because C/02 is a typed Gram matrix.

### Track D: GUDHI TDA (`W/D-tda`, commit 787d29a)

Commands: `PY W/D-tda/00_premise_checks.py` through `06_master_summary.py`, in order.

What it computed:
- Resolved Kummer Betti numbers (1,0,22,0,1), with chi 24 from both the f-vector and the Betti numbers, at N = 6 and 8 over Z/3 and Z/5.
- The RP^3 mod-2 torsion jump. The translation control passes.
- Künneth for K3 x T2: (1,2,23,44,23,2,1), chi 0.

Status:
- 11 clean, including `k3_second_betti_hodge` (partial), the T-duality rows (d <= 4) and `dualScale_*`.
- `D-tda#56` (Dirac index 2) was upgraded to AGREE.
- Qualified: `k3_euler_characteristic` and `k3_hirzebruch_signature`.

### Track E: flux vacua (`W/E-flux`, commit 3bdf02d)

Commands: `PY W/E-flux/scripts/tadpole_bookkeeping.py`, `tt_metric_checks.py`, `flux_enumeration.py`.

Status:
- 5 AGREE: `euler_T2`, `k3k3_anomaly`, `k3t2_euler_zero`, `tadpole_budget`, and `euler_K3` (qualified).
- Details in §5.

## 3. Rigidity

Label key:
- **RIGID**: a structural selecting condition, and a neighbouring value fails.
- **NORMALISATION**: the selecting condition contains the target.
- **CONDITIONAL / RIGID_GIVEN_DEFINITION**: rigid once the named inputs are granted.
- The other labels are not rigidity results.

| # | Track | Inserted parameter | Track's label | Final label | Depends on | Deciding evidence |
|---|---|---|---|---|---|---|
| 1 | A | N, the mu-term coefficient (scan 20..28) | RIGID | **NORMALISATION** | the typed k = 2 | `skA_scan_depends_on_k.py` and blindness `normalisation_k_test.py` agree independently: k = 1, 2, 3 selects N = 12, 24, 36, i.e. N = Z(tau,0) |
| 2 | A | k, selector Z(tau,0) = 24 | NORMALISATION | NORMALISATION | e(K3) typed | the selector is the literal target |
| 3 | A | k, selector (q^0,y^1) coefficient = 2 | NORMALISATION | NORMALISATION | 2 typed | the same |
| 4 | B | kappa, the Göttsche exponent | NORMALISATION | NORMALISATION | computed chi | both sides are the same function call (`part1_euler_numbers.py:88-90`) |
| 5 | B | N, the A_{2,1} coefficient | RIGID | **RIGID** | DMVV input 2 phi_{0,1} (k = 2); DMZ form 3E4A - M Hhat (tier L) | `skB_immortal.py`: exact solve of 58 equations gives (N, E4A coefficient, M) = (324, 3, 648) |
| 6 | B | M, the Hhat coefficient | RIGID | **RIGID** | the same, plus the Hurwitz table (Dirichlet cross-check) | same solve; the H(3) + 1/6 control has no solution |
| 7 | C | (m,n) in mU + n(-E8) | CONDITIONAL_ON_INPUT | **RIGID_GIVEN_DEFINITION** | K = O and b1 = 0 (the definition of K3); Noether and Serre (tier L); even unimodular rank-22 classification (tier L) | `skC_chain.py` reverse chain with no chi input forces chi_top = 24, so (3,19), so (3,2); (11,0) and (7,1) are rejected by signature, (11,11) and (7,15) |
| 8 | C | tadpole budget | NORMALISATION | NORMALISATION | chi_top | the budget is read from a file |
| 9 | C | generic A, G T-duality identities | RIGID | **VERIFIED_IDENTITY** | none | universal identities; no parameter is selected |
| 10 | D | grid size N | RIGID | **INVARIANCE** | none | `skD_premises.py 4`: open stars are disjoint at N = 4 and the Mayer-Vietoris output is (1,0,22,0,1). D's "N = 4 fails" is an over-strict closed-star criterion, so nothing is selected |
| 11 | D | k resolved points, selector chi = 24 | NORMALISATION | NORMALISATION | chi = 24 typed | chi(X_k) = 8 + k |
| 12 | D | k resolved points, selector b1 = 0 | NON-DISCRIMINATING | NON-DISCRIMINATING | none | all 17 values pass |
| 13 | E | tadpole 24 -> alpha^2 in {8,16,24} | NORMALISATION | NORMALISATION | TT (2.3), tier L | an input |
| 14 | E | 8 divides alpha_x^2 | RIGID | **RIGID** | TT (2.5) even quantisation (tier L); evenness of H^2(K3,Z) | `skE_tt_faithful.py`: det Gamma_{3,19} = -1 with all diagonal entries even, and alpha = 2v gives alpha^2 = 8 x an integer; control: the odd lattice I(3,3) gives alpha^2 = 4 |

**Totals.** 4 of 14 are genuinely rigid (5, 6, 7, 14). Both skeptics agree on only 2 (5 and 6). By final label: 7 NORMALISATION, 3 RIGID, 1 RIGID_GIVEN_DEFINITION, 1 VERIFIED_IDENTITY, 1 INVARIANCE, 1 NON-DISCRIMINATING.

**The two points where the skeptics disagreed**
- **Row 14.** The blindness skeptic said the result holds only because of the chosen basis and "fails under (A.2)-(A.3)".
  - What fails in the hyperbolic basis is reproducing TT's worked numbers: (4.15) gives alpha^2 = 0 there.
  - The divisibility does not fail. alpha = 2v with v in an even lattice gives 8 | alpha^2 in any basis.
  - The blindness objection is superseded by `skE_tt_faithful`. E's own negative control (rank-2 emptiness) tests the signature, not the factor 8, and is replaced by the odd-lattice control.
- **Row 7.** The blindness skeptic said C is conditional on D's chi.
  - `skC_chain`'s reverse chain shows chi_top is forced by K = O plus b1 = 0 (Serre duality gives h^{2,0} = 1, so chi(O) = 2, so c2 = 24).
  - The only remaining inputs are the definition of K3 and tier-L theorems.
  - C's own chained controls at chi = 36 and 48 are invalid: no compact complex surface with c1_R = 0 and b1 = 0 has those values (Enriques-Kodaira, tier L).

**What "zero free parameters" means here.** Once the defining structures are granted (K3 as a compact complex surface with K = O and b1 = 0, TT's even flux quantisation, and DMVV/DMZ built on Z = 2 phi_{0,1}), the integers 24, 22, (3,19), (3,2), 324 and 648, and the constraint 8 | alpha^2, admit no alternative. The overall normalisation k = 2 of the elliptic genus, and the tadpole total 24, are inputs to the blind computations, not outputs. Nothing here touches the cosmological sectors.

## 4. The chain D -> C -> lattice, and A/B/D consistency

**The chain as run**
1. D (GUDHI plus Mayer-Vietoris) gives chi = 24 and b2 = 22.
2. C reads chi_top from `D-tda/exports.json`.
3. Noether gives chi(O) = 2, so h^{0,2} = 1, so b2+ = 3 and b2- = 19, i.e. (3,19).
4. Among the rank-22 even unimodular candidates (11,0), (7,1) and (3,2), only (3,2) has that signature.

`chain/chain_check.py` reports 9 of 9 links CONSISTENT, but that is not the finding:
- Links 5, 6 and 8 are pointer checks: C reads D's file.
- Links 2 and 3 read the hand-written `B-dyons/results.json`.
- Links 7 and 9 are definitional lattice arithmetic.
- Only links 1 and 4 (A or B against D) are cross-method, and A and B share one theta route and the typed factor 2.
- So at most one link carries independent information.

**The stronger statement.** `skC_chain`'s reverse chain gets chi_top = 24 from K = O and b1 = 0 alone. C's step therefore does not need D's number. D's GUDHI value is an independent check that the triangulated Kummer resolution actually has that Euler number.

**Is D's Mayer-Vietoris step sound?** Yes (math skeptic, `skD_premises.py 4` and `6`):
- P1: open stars are disjoint at N = 4 and N = 6.
- P2: all 16 links have RP^3 homology (Z/2: 1,1,1,1; Z/3: 1,0,0,1).
- P3: the rank of H_3(links) -> H_3(U) is 15, computed from data.
- The output is (1,0,22,0,1).
- The one remaining tier-L input is b2(D(O(-2))) = 1.
- D's N = 4 rejection was over-conservative. The decision on this now overrides v1's N = 4 "invalid", which used closed stars.

**A/B/D consistency.** phi_{0,1}(tau,0) = 12 is computed (`normalisation_k_test_results.json`), and the typed k = 2 turns it into 24. So "the elliptic genus agrees with GUDHI" is exactly the statement that k = 2. Read the other way: D's blind chi = 24, plus the tier-L identity Z(tau,0) = chi, fixes k = 2 without typing it. That is a proposal for v3, not a result here. B's (324, 648) inherit the same k = 2.

## 5. Flux vacua (Track E)

**The finite list.** 8 | alpha_x^2 (row 14 of §3), the tadpole (1/2) N_flux + N_D3 = 24 (TT (2.3), lines 170-171), and N_flux = 2 alpha_x^2 (TT (4.13), re-derived symbolically) give:

| alpha_x^2 | N_flux | N_D3 |
|---|---|---|
| 8 | 16 | 16 |
| 16 | 32 | 8 |
| 24 | 48 | 0 |

This holds whether the lattice is read in the diagonal basis or the unimodular one.

**Counts in the truncation** (`E-flux/flux_enumeration_results.json`; box = coefficient bound N)
- Rank 2, signature (1,1): 0 pairs at every N. The reason is structural: alpha-perp has signature (0,1).
- Rank 4, signature (2,2), with the tadpole condition:
  - pairs 80 / 656 / 1616 / 3280 for N = 1..4;
  - split by alpha^2 = 8 / 16 / 24 at N = 4: 1032 / 520 / 1728;
  - no alpha^2 = 24 pairs at N = 1;
  - without the tadpole condition: 80 / 1136 / 5600 / 18592.
- Rank 6, signature (3,3), with the tadpole condition: 6960 (N = 1) and 339600 (N = 2). Without it: 6960 and 556944.
- Every count grows with N. The finiteness comes from the box, not from the tadpole condition.

**Caveats on these counts**
- The pairs are counts of an **index-8 (rank 6) or index-4 (rank 4) subfamily** of TT's literal (2.5) quantisation. TT's appendix basis claim is false over Z (details under the source errors below).
  - Example: at alpha^2 = 24, in the earlier skE_lattice box, 1728 pairs satisfy (2.5), and 192 of them are in E's family.
- The scope is TT's (4.8)-(4.10) example family ("As a concrete example", TT line 846), not all SUSY vacua with dim V_flux = 2.
- Orbit counts 3 / 13 / 30 / 57 (rank 4) and 15 (rank 6, N = 1) are **not certified upper bounds**:
  - 32 of the 64 group elements reverse the orientation of the positive block;
  - no physical identification group is specified. Diffeomorphisms act through O^+ (Borcea/Donaldson, tier L, recalled by the skeptic from memory).
- Only the raw pair counts inside the box are tier B.
- Primitivity (TT eq 3.34) and avoidance of orbifold singularities were not checked pair by pair.

**What stays unfixed**
- Complex structure: fixed (phi = tau = i) for this family.
- Kähler moduli: all 20 Kähler moduli of K3 (22 H^2 directions minus the 2 fixed by (3.34)), plus the T2 Kähler modulus, remain free (TT lines 843-845).
- 7-brane gauge and position moduli, and D3 positions: TT does not analyse them.

**Orientifold facts, with line references**
- TT lines 160-163: 4 T^2 fixed points carry 4 O7 planes, cancelled by 16 D7 branes.
- TT lines 164-171: 2 units of D3 charge per O7 and 1 per D7 give 24 (eq 2.3).
- TT lines 180-183, footnote: 24 (p,q) 7-branes x 1.
- TT lines 222-223: the flux coefficients are even integers.
- BLPSSW lines 204-214: n1 + n5 = 24; 16 hidden instantons at the orbifold singularities, plus 8 five-branes. So 16 + 8 = 24 is stated in words, and E's hedge (lines 966-970, "one might intuitively think") overstates the doubt. Identifying BLPSSW's 16 with TT's 16 D7 would be numerology.
- Independent re-derivation (`skX_rows_orientifold_dirac.py`):
  - 4 I_0* fibres, sum of e = 24 = e(K3) from D's Betti numbers;
  - 16 D7 and 4 O7, with Q(O7) = -4;
  - totals -16 and +16;
  - D3 = 24 = chi(K3 x K3)/24 = 576/24, split as 8 from the O7s and 16 from the D7s;
  - control: 16 O7 would need a sum of e equal to 96, which exceeds 24.

**Sealed-Lean errors**

| Sealed statement | Sealed value | Correct value |
|---|---|---|
| `TadpoleCancellation.lean:41` (O7 total) | -64: 16 O7 planes, i.e. the T^4 fixed-point count, which would be O5 planes | -16 |
| `:52` (D7 total) | 64 = 32 D7 x 2 (ratio -2) | 16 D7 x 1 (ratio -4) |
| `:68` (D3 target) | 1 = chi(K3)/24, the charge per 7-brane | 24 |

- The sealed corpus contradicts itself on the D3 target: `Flux/Tadpole.lean:99,116` use 24.
- `KummerTadpole.lean:126-127` has the right counts in a unit 4 times the D7 unit (NOT_COMPARABLE, matching).

**Errors in the source paper, TT**
- **Appendix basis claim** (lines 1819-1821, 1872 and 1880-1882): the claim that the (A.6) forms are a Z-basis is false over Z. Their span has index 8: |det C| = 8, and the Gram determinant is -64 against -1.
- **Line 941** ("N_flux = 24 and no D3-branes") is a prose slip. (2.3) needs 48, and TT's own line 1197 and eq (4.32) apply the factor 1/2 correctly. E called this unresolvable; it can be resolved.

## 6. Reverse predictions

Status: 3 of 3 held, 0 failed. The pass was asked for 5-8 items and completed 3. It read LeanMaster commits `4c4ad233` through `41d65194`; the quoted statements did not change between them.

**Item 1: `c_depends_only_on_D` extended from q^9 to q^20**
- Result: it holds with 0 conflicts. There are 22 new D values, and a +1 perturbation control has teeth.
- Engine: B's theta cache.
- Reachable in Lean now: **no**. It needs zK3 raised to `ellipticGenus 20`.

```lean
theorem c_depends_only_on_D_ext : (List.range 21).all fun n => (List.range 45).all fun i => let l : ℤ := (i : ℤ) - 22; (zK3.getD n LP.zero).coef l == cK3 (4 * n - l * l) := by decide  -- needs zK3 := ellipticGenus 20 first (currently ellipticGenus 9)
```

**Item 2: `twined_div24`, `twinedAll_div` and `moonshine_modules_decompose` extended to n = 10..20**
- Result: 286 structural checks hold.
- Engine: `moonshine_engine.py`, validated bit for bit against Lean's n <= 9 tables after two implementation bugs were fixed. Two perturbation controls both break the property.
- CDH Table 48 stops at n = 9, so these are new facts.
- Reachable in Lean now: **yes**. Substitute 20 for the hard-coded 9; there is no dmvv truncation to raise.

```lean
theorem moonshine_modules_decompose_ext : (List.range 21).all (fun n => (List.range 26).all fun i => let v := moonshineInner n i; v.2.1 == 0 && v.2.2.1 == 0 && v.2.2.2 == 0 && 244823040 ∣ v.1 && 0 ≤ v.1 / 244823040) = true := by decide +kernel
```

**Item 3: immortal dyons at m = 4**
- Claim: Phi_{2,4}^opt = -H|V4 + 2 H|U2, with the full divisor-sum V4.
- Result: 68 keys with zero mismatches. The naive two-term form fails, and so does DMZ's (9.14)-style factor-2 variant.
- **Caveat (mine, from the committed JSON; neither skeptic reviewed the reverse pass):**
  - The match that holds uses psi_{0,5}^opt quoted from DMZ Table 2 (tier L).
  - The DMVV route has `holds_correct_formula_via_G5_DMVV: false`. G_5/A is Delta psi_4, a different object.
  - The return value's "independently computed from the K3-elliptic-genus DMVV product" therefore overstates the result. The accurate reading: tier-B arithmetic consistency between two DMZ quotes, using the blind Hurwitz table. G_5 is used only for the p24(5) = 176256 gate.
- Reachable in Lean now: **no**.

```lean
theorem dmz_913_hecke_m4 : Ser.eqB (divByA (Ser.sub psiOpt5 (Ser.scale 248832 (aTimesA2m 4 2))) 2) (Ser.sub (Ser.scale (-1) (hurwitzV4FullSer 4 2)) (Ser.scale (-2) (hurwitzU2Ser 2))) = true := by decide +kernel  -- needs hurwitzV4Full (true divisor sum, not hurwitzVSer's two-term pattern), psiOpt5, and dmvv 6 2 / ellipticGenus N>=12 first
```

Commands: `cd W/reverse && PY item1_index1_q20.py`, then `PY item2_moonshine_q20.py`, then `PY item3_immortal_m4_hecke.py`. Items 1 and 3 need `cd W/B-dyons && PY theta_forms.py 40 && PY hurwitz_class_numbers.py` run first.

These are proposals only. None has been built in Lean, and each must pass the LeanMaster proof gate before anyone calls it verified.

**Handover to LeanMaster**
- Items 1 and 2 above.
- A wrong sign-off to retract: the v3.20.0 `TadpoleCancellation.lean` values. v3.21.0 already fixes them.
- A candidate: an integral statement that the TT (A.6) span has index 8 in H^2(T^4,Z).

## 7. Not done, disputed, or from memory

### 7.1 Ground-rule violations (both skeptics)

- **Forbidden wording.** Three tracks labelled tier-B arguments as established by proof:
  - E-flux: `results.json:60` and its return value (the rank-2 emptiness argument, which is itself correct);
  - C-lattices: its return value, and `results.json:232`;
  - B-dyons: its return value.
- **Rigidity labels.** C's T-duality identities and E's 8 | alpha^2 were labelled RIGID with the wrong control; see §3.
- **Literals in computation paths:**
  - A: `run_track_a_v2.py:81,209` types the factor 2; `:322` onward types 24 as the mu coefficient instead of using the scan's solution.
  - B: `theta_forms.py:135` types the factor 2 in `twoB`.
  - E: `tadpole_bookkeeping.py:72-76,151-155` is quotation arithmetic reported as computed; it is tier L. `orifold_singularities=16` is typed instead of read from D.
- **Reproducibility:**
  - `B-dyons/results.json` and `E-flux/results.json` are hand-authored; no script generates them.
  - `chain/chain_check.py` fails from a clean checkout (FileNotFoundError at line 53).
  - C, D, E and chain hard-code the absolute worktree path.
  - B's stated commands use bare `python`, and the part1/part2 docstrings say `theta_forms.py 16` although the committed run used 40. The full commands are given in §2.
- **B grid.** The (N,M) grid in `part3_polar.py:260-261` is centred on the answer and samples even M only. Uniqueness rests on skB's exact solve instead.

### 7.2 Mathematical corrections

- The sealed TadpoleCancellation errors and TT's appendix and line-941 errors (§5).
- A's RIGID {24} is an echo of the normalisation.
- C's chi controls use surfaces that do not exist.
- E's orbit "upper bounds" are unsupported.
- D-tda#56 should be AGREE.

### 7.3 Track could_not_do (condensed)

- **A:**
  - no literal-free selector for k;
  - the sign-pattern check was left open;
  - chi(2A) was taken as input, not re-derived.
- **B:**
  - Delta psi_m for m >= 3 was not checked;
  - the Kronecker-Hurwitz validation relation is tier L. The math skeptic's Dirichlet route now cross-checks the table.
- **C:**
  - the lambda-in-R T-duality check was carried over from v1, not redone;
  - the Kähler relation b2+ = 2h^{2,0} + 1 is assumed. The math skeptic notes the step needs only that b1 is even (tier L);
  - the entry-level symbolic tie-back was done only at d = 2.
- **D:**
  - the parity criterion on the intersection form was not evaluated, because there is no chain-level pairing;
  - b0, b3 and b4 are stated. Superseded in part: skD_premises computes the H_3 map rank, giving b3 = 0 and b4 = 1 from the MV sequence;
  - N >= 10 was not tried;
  - the diffeomorphism of the links with RP^3 is not checked (tier L).
- **E:**
  - rank-6 orbit counts at N = 2 were not computed;
  - there is no quotient by the full Aut(Gamma_{3,3});
  - TT §4.2 and §5 families were not enumerated;
  - primitivity and singularity avoidance are taken on TT's word.
- **Reverse:** only 3 of the 5-8 requested items were done. Umbral exactness beyond Lean's range and a second TT cross-check were not attempted.

### 7.4 NOT_COMPUTED (207 rows) by kind

- M24 character-table statements, including group-trace LHS sides.
- Twisted and twined DMVV.
- Kummer code, Golay code and octad.
- Moore's form-count identity to D <= 400.
- Hurwitz values for D > 40.
- DMZ m = 3 and m = 4 lines.
- Umbral forms other than Z^(2).
- Plus the two demoted Göttsche rows.
- See the `rows` field in the ledger for the per-row note.

### 7.5 From memory or tier L, unverified here

- CDH chi(g)/F_g: recalled from memory, but structurally re-derived by skA.
- Borcea/Donaldson O^+.
- Enriques-Kodaira classification.
- Froehlicher degeneration.
- BHPV IV.2.7.
- Noether and Serre duality.
- Milnor's classification of even unimodular lattices.
- b2(D(O(-2))) = 1.
- DMZ Table 2's psi_{0,5}^opt.
- DMZ (5.16).

### 7.6 Process

- **Seal.** The sealed targets sat outside the worktree during the blind phase. The forbidden-path scan is clean. No blind-track file changed at or after comparison commit `3a5096c`.
- **Post-seal change.** LeanMaster changed three sealed statements (`adc85e7`).
- **Shared-index sweep.** Commit `787d29a` swept Track C's staged files into a Track D commit. The content is byte-identical; only the attribution is wrong.
- **Prior results file.** It stays at `/mnt/disks/disk-socrateai-local-1/k3t2-sealed-v2/_prior_results_wf_ca8ac00f.json`. It was not copied into the worktree, and the ledger builder does not read it.
- **Skeptic reruns.** Both skeptics' reruns reproduced byte-identical output. The exception is the chain script, which fails from a clean state (§7.1).

**Files**
- `W/agreement_ledger_v2.json` and `W/build_ledger_v2.py` (commit `9e34ce8`, run as `PY W/build_ledger_v2.py`).
- `W/comparison.json`.
- `W/skeptic_math/verdict.json` (commit `97c0b5a`).
- `W/skeptic_blindness/verdict.json` (commit `200a374`).
- `W/reverse/REVERSE_PASS_V2_FINDINGS.md`.
