Generated by workflow reverse-to-zero; exploratory (tier X) unless stated; M0 is a hypothesis change, not a derivation.

# Reverse-to-zero: final report (run wf_32d1ef7e, resumed after the crash)

**Where things are.** Worktree `/mnt/disks/disk-socrateai-local-1/dualscale-wt-reverse`, branch `loop/reverse-zero`, not pushed. This synthesis added one commit, **5b6302e**, containing the ledger `/mnt/disks/disk-socrateai-local-1/dualscale-wt-reverse/audit/reverse_zero/ledger_reverse_zero.json`. The ledger is written by `/mnt/disks/disk-socrateai-local-1/dualscale-wt-reverse/audit/reverse_zero/synthesis/build_ledger.py`.

**How to rebuild the ledger.** The script is deterministic and uses no random numbers, so it has no seed. The command is:
`/home/callensxavier_gmail_com/SocrateAI-Scientific-DualScaleSimulator/.venv-tda/bin/python /mnt/disks/disk-socrateai-local-1/dualscale-wt-reverse/audit/reverse_zero/synthesis/build_ledger.py`

**Where the numbers come from.**
- Every number below is read by that script from the committed result JSONs of this branch.
- Quotes from PRE_REGISTRATION.md are asserted line by line against the read-only main-repo file.
- LeanMaster quotes are asserted verbatim against the committed tree at **eb791e7**, read with `git show`, so read-only.
- Nothing in PRE_REGISTRATION.md, LeanMaster, proofs/ or main was modified.
- `_prior_results_wf_32d1ef7e.json` was left untracked.

---

## 1. Bottom line

**Parameter count.** This is the single count used throughout the report.
- **M0: 0 free theory parameters, by hypothesis change.** The symmetron sector is deleted, so `mu_sym` has no value. `c4_pta_product` is set to its GR value, 0. Ω_Λ is frozen at 0.68885.
- That 0.68885 is an *imported* number: the docstring at `DualScaleCosmology/DarkEnergyScale.lean:83-84` reads "`Ω_Λ` from `astropy.cosmology.Planck18` (Planck 2018 VI, Table 2), rounded". It is tier L, not derived.
- M0 still carries **two nuisances that are profiled out analytically**: the BAO scale r_d·h and the supernova (SN) absolute offset (H0/M_B). H0 cannot be frozen: PRE_REGISTRATION.md:27 (R1) records "**Rejected: Δχ² = +1204.5**".
- **M2: 2 free parameters** (`mu_sym`, `c4_pta_product`). Both are **untested by any dataset in hand**. They are not bounded, not derived, and not shown to be zero.
- Nothing here derives M0 from K3×T².

**What the data say about going from M2 to M0.**
- **The fit cannot tell M0 and M2 apart.** Both have χ² = 1401.071, because neither parameter enters any term of the likelihood.
- M2 is penalised only by the parameter count: ΔAIC(M2−M0) = +4.0 and ΔBIC = +14.75. That is parsimony bookkeeping, not evidence. The data say nothing about `mu_sym` or `c4`.
- **M0 against a ΛCDM fit with Ω_m free**, on DESI DR2 BAO + Pantheon+ with full STAT+SYS covariance (1580 SNe): Δχ² = 0.746 (1 dof), which is **0.86σ** (chi2.sf = 0.388). ΔAIC = −1.25, ΔBIC = −6.63.
- This comparison was **not pre-registered**. The 9.00 is borrowed from the threshold table at PRE_REGISTRATION.md:35, and the precedent R2 is labelled "reported, not pre-registered".
- Negative controls, as Δχ² against the fitted model: Ω_Λ = 0.50 gives 364.0; Ω_Λ = 0.65 gives 28.9; Ω_Λ = 0.72 gives 10.1. Including the 77 calibrator SNe (1701 SNe total) gives 0.0094.
- The *background* of M0 is Planck-ΛCDM, so this result means "consistent with Planck-ΛCDM". It does not confirm anything specific to M0.
- **E1 (P1 pipeline action).** DR2: Δχ²(CPL vs ΛCDM, 2 dof) = **4.547, which is 1.63σ**. Best fit w0 = −0.896, wa = −0.186. Details in §2.
- **E2 (PTA).** No angular Hellings–Downs (HD) table with covariance exists (ABSENT, see §6), so `c4_pta_product` remains untested and nothing changes.
  - The exact scan gives a slope of **1.000**. The value 6.171 reported in E2_RESULT.md:78 is wrong (`skeptic_statistics/check_e2_slope.json`).
  - The precision needed from any future angular dataset is |c4| = 0.00648, 0.0324 or 0.0648, corresponding to perturbations of 1%, 5% or 10% of the HD dynamic range (0.648).

## 2. Pre-registered results

### P1 (DESI DR2)

**The rule, verbatim.**
- PRE_REGISTRATION.md:40-43: "**Falsified if** any of these, analysed with full published covariances, prefers the w₀ > −1, wₐ < 0 quadrant over w = −1 at **≥ 5σ (Δχ² ≥ 28.74, 2 dof)**:" followed by "DESI DR3 or final BAO combined with CMB and any one major SN compilation;" / "Euclid;" / "Rubin/LSST supernovae."
- Line 45: "**Also falsified if** a ΛCDM fit to those data puts Ω_Λ away from 0.68885 by **≥ 5σ** of the measurement error."
- Lines 46-48: "**Pipeline action, to be run with these thresholds fixed now:** … re-run `decisive_experiment.py` with DESI DR2 BAO and the full Pantheon+ covariance; … report the CPL-vs-Λ Δχ² as it comes out."

**Applying it.**
- DR2 BAO + SN with no CMB is not one of the datasets the rule names. So **the falsification rule does not apply to this data.** What applies is the pipeline action, which **was executed**.
- P1 has no 3σ bar. Earlier text that says "P1 NOT rejected; 3σ and 5σ bars missed" misapplies the rule and is withdrawn.

**Results, tier X.**

| Dataset and covariance | SNe | Δχ² (2 dof) | σ |
|---|---|---|---|
| DR2, full covariance | 1590 | 4.547 | 1.63 |
| DR2, full covariance | 1580 (calibrators dropped) | 4.735 | — |
| DR1, full covariance | 1590 | 3.191 | 1.27 |
| DR1, diagonal errors | 1590 | 6.091 | — |
| DR2, diagonal errors | 1590 | 8.467 | — |

- The DR1 diagonal-error run reproduces R3's recorded 6.09 exactly. Using the full SN covariance roughly halves the preference, so **R3's 2.0σ was inflated by ignoring the SN covariance.**
- ΛCDM fit to DR2: Ω_Λ = 0.6960, which is 0.887σ from 0.68885.
- The published 3.1σ result (DESI DR2 + CMB + SN) is **not reproduced**, because no CMB likelihood was used (NOT ATTEMPTED, see §6).
- E1 used a new script, `E1-desi-dr2/e1_desi_dr2.py`, not the `decisive_experiment.py` named at line 47.

### P2 status, in the light of LeanMaster Stream 6

- LeanMaster P1 (the extra-dimension radius) is *not* the same as PRE_REGISTRATION P1 (the cosmological constant).
- STREAM6_EXPERIMENT_PLAN.md:74-77 (eb791e7) excludes R = s ≈ 47.0 µm as the radius of an extra dimension.
- STREAM6:80-83 gives κ = 1, but only "with `α' = s²` (the Tier C dual-scale identification of Stream 3)". That removes κ as an O(1) knob *within* that Tier C identification.
- κ = 1 does not remove the (8π)^{1/4} Planck-mass-convention spread that makes up the registered bracket (PRE_REGISTRATION.md:53).
- PRE_REGISTRATION.md:56 already says: "The central value 47 µm is therefore already above the bound. Only the lower part of the bracket survives."
- The P2 rule (line 58: an α = 1 Yukawa deviation excluded "for every range ≥ **21.0 µm** at 95% CL") is **not triggered**. No new short-range gravity data were analysed, and the Eöt-Wash α–λ table is ABSENT.
- I do not quote a surviving interval. Lines 55-56 cite bounds on *different quantities* (a 30 µm toroidal radius, a 38.6 µm Yukawa range, 44 µm from MVV). The figure "21.0-38.6 µm" in m0_result.json:135 mixes them and is withdrawn.

### Proposed ADDENDUM for PRE_REGISTRATION.md

This is to be appended by the orchestrator. It leaves lines 1-84 unchanged.

> ## Addendum 2026-09-19 (appended; original rules above unchanged)
> Source: simulator branch `loop/reverse-zero` (E1 commit 4eba061, E3 commit 0c590a5, skeptic commits 240e421/1d42c16/e5cf853, ledger 5b6302e); LeanMaster read-only at `eb791e7`.
>
> **A1. P1 pipeline action (lines 46-48) executed.** DESI DR2 BAO (ALL_GCcomb, 13×13 cov) + Pantheon+SH0ES full STAT+SYS covariance, 1590 SNe, no CMB, script `audit/reverse_zero/E1-desi-dr2/e1_desi_dr2.py` (not `decisive_experiment.py`). CPL vs Λ, Ω_m free in both: Δχ² = 4.547 (2 dof, 1.63σ), w₀ = −0.896, wₐ = −0.186. ΛCDM fit Ω_Λ = 0.6960 (0.89σ from 0.68885). The falsification rules at lines 40-45 name DESI DR3/final BAO + CMB + SN, Euclid, or Rubin; they are **not applicable** to this dataset, and no verdict on P1 is recorded from it. The published DESI DR2 + CMB + SN 3.1σ (R4) is not reproduced (no CMB likelihood used).
>
> **A2. R3 superseded in accuracy (R3 kept as the historical record).** Same DR1 BAO and same 1590-SN cut: diagonal SN errors give Δχ² = 6.091 (reproduces R3), full STAT+SYS covariance gives Δχ² = 3.191 (1.27σ). R3's 2.0σ is an artefact of neglecting SN covariance.
>
> **A3. Reported, not pre-registered: M0.** M0 = frozen flat ΛCDM with Ω_Λ = 0.68885 (imported Planck18 value), symmetron sector removed, c4_pta_product = 0 (GR). It is a hypothesis change, not a derivation from K3×T². It carries two profiled nuisances (r_d·h, SN offset); H0 is not frozen (R1). DESI DR2 + Pantheon+ full cov (1580 SNe): Δχ²(M0 − fitted ΛCDM) = 0.746 (1 dof, 0.86σ).
>
> **A4. P2.** No new short-range gravity data analysed; the rule at line 58 is not triggered. LeanMaster Stream 6 (STREAM6_EXPERIMENT_PLAN.md:74-83 at `eb791e7`) excludes R = s ≈ 47 µm as an extra-dimension radius and gives κ = 1 under the Tier C identification α′ = s²; this does not remove the (8π)^{1/4} convention spread of line 53.
>
> **A5. No TDA threshold is registered in this file.** The TDA thresholds used on 2026-09-19 (ξ(r) gate RMS z < 3, LOO 95th percentile, Bonferroni over 6) were fixed in script source, not here. Any future TDA or defect test enters this file, with statistic, null, seeds and decision rule, before its data are loaded.

## 3. TDA: cosmic web and CMB

**Headline: no topology survives its own controls.**

### Cosmic web (SDSS DR17, 193,536 galaxies, 0.02 ≤ z ≤ 0.12)

**Earlier runs.**
- E2-TDA: 8 draws of N = 1200 per side. Lower total persistence in H1/H2 than the redshift-shuffle null (Welch p = 3.6e-4 and 3.3e-5).
  - Those p-values are pseudo-replicated: the draws overlap.
  - The gap tracks the mean nearest-neighbour distance (p = 2.6e-9), so it is ordinary two-point clustering, not new topology.
- E4 (N = 400): real and shuffled data tie at Betti numbers [1, 0].

**E5b known-answer controls.**
- Planted voids: 6 out of 6 recovered, with deaths within 0.95% of r = 15. This test uses a clean box whose density does not match the data.
- Circle control: dominant ratio 33.97.
- Both were rerun twice by the statistics skeptic with identical results.

**E5b nulls.**
- **Poisson-in-footprint:** 20 realisations of N = 25000 each, on a HEALPix nside = 64 mask.
  - Per-shell selection is not matched: the shells hold 6197 / 6273 / 6187 / 6343 points against 6250 each for the data. A KS test on r gives p = 0.009.
  - The mask pixel spans 2.6-3.7 Mpc/h across the shell.
- **22 of 22 CAMB lognormal mocks** built, with bias fitted on ξ(r) only over 20-60 Mpc/h: b = 0.893.

**Result.**
- The Betti-curve rank p-value is at its floor, 1/23 = 0.0435, for H0, H1 and H2, with margins of 33.8 / 17.9 / 10.2.
- **The whole excess sits at r in [2, 20) Mpc/h.** At r in [20, 40] every curve, real and mock alike, is [1, 0, 0] (p = 1.0).
- **Verdict: INCONCLUSIVE.**
- The ξ(r) gate cannot rescue this. An unclustered catalogue (ξ = 0) passes the gate with RMS z = 1.73 against a threshold of 3, and any rescaling of the mock ξ from 0 to 3× also passes. The gate therefore has no power; a diagonal χ² does separate the cases (13.9 for mocks against 59.7 for Poisson).
- The gate threshold was "fixed in source before the first run", not pre-registered. The bias is also fitted and then gated on the same range.
- The confounds at r < 20 Mpc/h go beyond the 2 Mpc/h smoothing:
  - the mocks are in real space while the data are in redshift space (fingers-of-god);
  - the mask pixels are coarse.
- **On the conflicting statements:** `e5b_lognormal_and_poisson_null_report.json:1062` says "the actual M0 test", while commit dca9534 says it is not an M0 test. The commit message is right: with the signal confined to the range the mocks and the gate do not validate, this comparison tests the pipeline, not M0.

### CMB (WMAP 9-year ILC, KQ85 mask, nside 128, N = 100 Gaussian sims)

**Raw results.**
- Calibration ratio (variance only) = 0.977.
- Raw p-values:

| Statistic | Sublevel | Superlevel |
|---|---|---|
| b0 | 0.065 | 0.088 |
| b1 | 0.432 | **0.0109** |
| euler_chi | 0.305 | 0.205 |

- Bonferroni over 6 tests turns the superlevel b1 value into 0.065.

**The null does not match the data.** The masked pseudo-C_ℓ of the data divided by that of the simulations:

| ℓ band | data/sim | z |
|---|---|---|
| 151-200 | 0.92 | −6.6 |
| 201-256 | 0.81 | −11.6 |
| 257-383 | 1.41 | +11.0 |

- Probable causes, attributed from reading the code rather than measured:
  - the spectrum stops at lmax = 256;
  - `pixwin=False`;
  - the mask is applied twice.
- A recalibration failed its own pre-stated gate (max |z| = 4.12, gate rule written before the results), so **no corrected p-values exist**.

**Two directions to keep together.**
- The mismatch gives the simulations extra small-scale power, which raises their b0/b1 counts. That has the same sign as the negative residual in superlevel b1, so a matched null would probably make the non-rejection safer.
- But Bonferroni over 6 is fragile here: euler_chi = b0 − b1, and sublevel and superlevel come from one map. With about 2 effective tests the corrected p is **0.022**.

So "M0's Gaussianity not rejected" is **not established at the claimed strength**. It awaits a null matched to the data spectrum.

**Toy cosmic-string sensitivity.**
- Detection fraction over the 95th-percentile leave-one-out threshold:

| Gμ | Fraction detected |
|---|---|
| 3e-7 | 0.05 (the nominal false-positive rate) |
| 1e-6 | 0.15 |
| 3e-6 | 1.00 |
| 1e-5 | 1.00 |

- The 95% bracket is (1e-6, 3e-6], which is **6.7-20×** weaker than the imported `planckGmuBound` = 1.5e-7 (`CosmicString.lean:83`).
- This is a pipeline sensitivity, not a bound. It is calibrated only on sublevel euler_chi.

### What this says

- **About M0:** nothing here rejects M0's topology assumptions (Gaussian CMB, ΛCDM clustering). Neither test is yet matched or powered well enough to support that.
- **About K3×T²:** nothing. LeanMaster STREAM8 says "Observables: none" (lines 112, 153, 374 at eb791e7). No TDA prediction exists or is attributed to LeanMaster.
- The sentence "K3×T² makes no … prediction … beyond the same Gaussian statistics" at `e5_cmb_tda_report.json:2067` should read "K3×T² makes no CMB-topology prediction."

## 4. LeanMaster constraints (verbatim at eb791e7) and what they close

**The quotes.**
- STREAM6_EXPERIMENT_PLAN.md:74-77: "`R = s ≈ 47.0 μm` fails all three pre-registered tests: T1 (Eöt-Wash toroidal radius `< 30 μm`), T2 (Yukawa range `< 38.6 μm`), T3 (MVV neutron stars `< 44 μm`) … By the pre-registered rule, **P1 is excluded** (`p1_verdict_excluded`)". The theorem is at Stream6Verdict.lean:53.
- STREAM6:80-83: "Within the programme's own T-duality `R ↦ α'/R` with `α' = s²` (the Tier C dual-scale identification of Stream 3), the self-dual radius is the unique positive fixed point `R = s` … this gives **`κ = 1`**". Theorems at :68 and :76.
- STREAM6:90-93: "**the dual-scale hypothesis as an extra-dimension or UV/IR statement does not survive the existing data.** … A new hypothesis would need a new, independently derived observable, frozen before any comparison (P1′), not a re-tuning of P1."
- STREAM7_HYPOTHESIS_INVENTORY.md:83: "**C-A is excluded** by the pre-registered rule". Lines 85-86: "both the extra-dimension reading and the Hubble-scale holographic reading of the dual-scale idea are now closed by recorded, kernel-checked verdicts." Theorem at Stream7CA.lean:54.
- STREAM7:94-95: "**not falsifiable in practice** (`cb_not_testable`)". Theorem at Stream7CB.lean:72.
- STREAM7:108-111: "The common cause is the dual-scale identification `α' = ℓ_P · c/H₀` itself: it either conflicts with data or pushes every stringy signal below any conceivable sensitivity. A new hypothesis worth testing would have to drop or replace that identification — and, for a realistic universe, use a geometry with a chiral spectrum (K3 × T² gives `N = 4` in four dimensions)."
- STREAM8_WHICH_K3.md:15-16: "A choice must be forced, not fitted."
- STREAM8:101: "**under trapping, T² comes to rest at `(ω, ω)`**". Theorems `roots_ww` at SelfDualT2.lean:79 and `ww_root_lattice_is_A2` at :109.
- STREAM8:112-113: "(iii) Observables: none — `N = 4`, non-chiral. Any consequence would be frozen before comparison (paper 11 protocol)."
- STREAM8:134-136: "The stabilizer of the octad in `M₂₄`, `(ℤ₂)⁴ ⋊ A₈`, acts on the complement as the affine group `Aff(𝔽₂⁴)` … TW's overarching group `(ℤ₂)⁴ ⋊ A₇` (order 40320, a maximal subgroup of `M₂₃`)". This corrects the earlier misquote. Theorem `octad_is_kummer` at KummerE3.lean:186.
- STREAM8:153: "E3 is structural, not dynamical. (iii) Observables: none (`N = 4`, non-chiral)."
- STREAM8:272-273: "No observable follows (`N = 4`). Anything that becomes a prediction would be frozen first (paper 11 protocol)."
- STREAM8:369: "*Not for the vacuum.* The attractor fixes the near-horizon moduli of one black hole." Line 374: "Observables: none (`N = 4`)."
- Tadpole.lean:116 and :40-42: "Tripathy–Trivedi eq. (2.3), `½ N_flux + N_D3 = 24`" … "nothing about whether a flux configuration realizing any particular `(flux, n)` pair actually exists — `tadpole_budget` is pure arithmetic on an assumed equation, not a construction."

**What they close.**
- The extra-dimension reading, the holographic reading and the relic-graviton (meV string scale) reading are all closed.
- Any route that keeps α′ = ℓ_P·c/H₀ inherits those failures.
- Streams 4-8 give dimensionless, structural results with no observable.

**What they do not do.** None of them fixes `mu_sym`, `c4` or Ω_Λ. LeanMaster Streams 6-8 do not mention `mu_sym` or `c4_pta_product` at all; calling them "untested" is this project's statement, not LeanMaster's.

**Scope of this check.** Theorem names are verified to exist at the cited lines. No kernel or axiom audit was run.

## 5. Proposed improvements

Every item here is **Tier C** as a proposal. They are ranked by whether they could yield a falsifiable number.

**1. (d) Make the self-dual / dark-energy scale a derived number.**
- *Why first:* PRE_REGISTRATION.md:80-81 names deriving "the O(1) factor in the dark-energy scale (P2)" as one of the two things that would make the theory falsifiable.
- *Builds on:* `rhoLambda_inv_eq` and `darkEnergyLength4_bracket` in DarkEnergyScale.lean, which put Λ^{-1/4} in [85, 90] µm, together with Ω_Λ.
- *The problem:* that identity only relabels ρ_Λ ∝ Ω_Λ·H0² using an imported Ω_Λ, so it is circular in H0 and Ω_Λ.
- *What is needed:* a dynamical derivation of Ω_Λ, or of ρ_Λ in Planck units, together with the Planck-mass convention factor. It must **not** use α′ = ℓ_P·c/H₀, which Stream 7 closes.
- *Observable:* Λ^{-1/4} in µm, or Ω_Λ (dimensionless, but it fixes ρ_Λ in eV⁴ once H0 is given).
- *Freeze plan:* derivation, then a Lean statement, then the number added to PRE_REGISTRATION before DESI DR3 / Euclid.
- *Difficulty:* this is the cosmological-constant problem, so open research. There is no known route.

**2. (c) TDA defect searches as null tests of M0.** Feasible now, but they test M0 only, not K3×T².
- *Fixes needed:*
  - a null matched to the data spectrum: lmax ≥ 3·nside, `pixwin=True`, a single mask pass, ideally MASTER deconvolution, and Planck SMICA alongside WMAP;
  - a pre-registered effective number of tests, with the superlevel b1 statistic included in the string-sensitivity calibration;
  - redshift-space lognormal mocks, a finer footprint mask, and a gate that uses full covariance (χ² < χ²₀.₉₅) on a range disjoint from the bias fit.
- *Observables with units:* the string-step amplitude in µK (reported as Gμ), and void and bar death radii in Mpc/h.
- *Builds on:* `planckGmuBound` (CosmicString.lean:83), used only as a comparator.
- *Freeze plan:* A5 of the addendum.
- *Difficulty:* moderate, weeks.

**2b. (extra) Turn `c4_pta_product` from untested into bounded.**
- *Data:* the NANOGrav 15-year raw timing data are **on disk** at `/mnt/disks/disk-socrateai-local-1/NANOGrav15yr/NANOGrav15yr_PulsarTiming_v2.1.0`.
- *What is needed:* a scoped `enterprise_extensions` run to produce binned Γ(θ) with its covariance.
- *Target precision:* |c4| = 0.0065 / 0.032 / 0.065.
- *Observable:* Γ(θ), dimensionless correlation against angle in degrees.
- *`mu_sym`:* it first needs a unit bridge, which does not exist.
- *Limits:* this bounds a nuisance; it derives nothing.
- *Difficulty:* moderate.

**3. (b) Flux-vacua statistics (Tripathy–Trivedi tadpole 24) as a landscape prediction.**
- *Input:* external, commit ab66bff (Track E), tier B inside its truncation.
- *The finite list* from 8 | α², N_flux = 2α² and ½N_flux + N_D3 = 24:

| α² | N_flux | N_D3 |
|---|---|---|
| 8 | 16 | 16 |
| 16 | 32 | 8 |
| 24 | 48 | 0 |

- *Pair counts with the tadpole condition:* 80 / 656 / 1616 / 3280 for rank 4 at N = 1-4, and 6960 / 339600 for rank 6 at N = 1-2.
- *Caveats on the counts:*
  - the counts grow with the coefficient box, so **the finiteness comes from the box, not the tadpole**;
  - they cover an index-8 subfamily, from TT's §4.1 example family only;
  - the orbit counts are not certified upper bounds;
  - 20+1 Kähler moduli stay free.
- **Observable with units: none.** A landscape prediction needs both a measure on the vacua and a unit-bearing observable per vacuum (for example a gravitino mass in eV after Kähler stabilisation). Neither exists, and Tadpole.lean:40-42 says the budget is "not a construction".
- *What is needed:* full-lattice enumeration modulo a certified O⁺(Γ) duality group, moduli stabilisation, then a distribution frozen before comparison.
- *Difficulty:* high.

**4. (a) Observable from Stream 8** (T² at (ω, ω), the Kummer 8 + 16 split, Mukai classes).
- **Observable with units: NONE EXISTS.** Every Stream 8 quantity is a dimensionless integer or lattice datum: 6 roots, A₂, 2⁶, the octad split, the 9 Mukai classes, 25353 states.
- LeanMaster itself says "Observables: none" or "No observable follows" in four places (S8:112, 153, 272, 374).
- The vacuum-selection reading is weakened in the committed text: non-symplectic symmetry points to i (S8:318), and the attractor argument works "Not for the vacuum" (S8:369).
- The only scales in the programme enter through α′ = s², which Stream 7 closes.
- *What would be needed:* a chiral geometry (a K3-fibred CY3) with explicit moduli stabilisation (STREAM7:108-111).
- *Freeze plan:* paper-11 protocol.
- *Difficulty:* research-level.

## 6. ABSENT, not attempted, disputed, and from memory

**ABSENT.**
- **NANOGrav HD angular table.**
  - Sources tried: nanograv/15yr_stochastic_analysis figure_1, PTArcade, Zenodo searches.
  - Re-checked this session: `…/correlations/narrowband/README.nb_correlations` describes per-pulsar *timing-parameter* correlations, not inter-pulsar angular correlations.
- **Eöt-Wash α–λ table.** Tried https://arxiv.org/e-print/2002.11761; the tarball holds only .tex source and 9 figure PDFs.

**NOT ATTEMPTED** (neither fetched nor ABSENT).
- A CMB likelihood for E1.
- A literature galaxy-bias table for E5b (b = 0.893 is not cross-checked).
- A kernel/axiom audit of the cited LeanMaster theorems.

**DISPUTED, with the two skeptics reconciled.**
- **`ground:leanmaster-streams6-8`.** The theorem names and line pins verify (statistics skeptic). The quoted prose is paraphrase (framing skeptic).
  - That prose has the M₂₄ octad stabilizer wrong (A₇ should be A₈).
  - It drops the Tier C qualifier from P6.2.
  - It confuses the two P1s.
  - It puts an invented "TDA prediction … frozen as Tier C" in LeanMaster's mouth.
  - Both skeptics are right. This report uses only the verbatim quotes in §4.
- **The invented "TDA prediction" and "(H′) … frozen before comparison" text is still in committed files:** IMPROVEMENT_PROPOSALS.md:80-82 and :99-102, cosmic_web_tda.py:244 and :268, e4 report :58-59, and IMPROVEMENT_PROPOSAL.md:16.
- **Committed files not yet corrected:**
  - "~0.1σ" at E3_RESULT.md:40;
  - "pre-registered 9.00" at m0_model.py:295;
  - "cleared decisively" at IMPROVEMENT_PROPOSAL.md.
- E5b: INCONCLUSIVE stands; "gate confirms mock recipe" does not.
- CMB: the numbers reproduce, but the null is mismatched (§3).
- E2: the slope is 1.000, not 6.171.
- E4: 2 of 35 JSON values drifted on rerun (circle ratio 29.858 vs 29.693; the cause was not isolated).
- E1 uses 1590 SNe and E3 uses 1580. The DR2 Δχ² is 4.547 and 4.735 respectively, so the verdict does not change.

**Observed but not citable.**
- LeanMaster's working tree has an uncommitted `DualScaleDyons/K3Enhancement.lean` and a modified `STREAM8_WHICH_K3.md` §12 (P8.4c: trapping gives SO(44), not a product point). This is expected: another session owns that repo.
- It is not at any commit and not gated, so it is excluded from this report. Re-read it at the next LeanMaster release.

**From memory, not checked.**
- The instrument dates in PRE_REGISTRATION's "M" column.
- The Borcea/Donaldson O⁺ remark, which Track E's skeptic recalled from memory.
