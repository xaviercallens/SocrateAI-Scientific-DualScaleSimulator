Generated by workflow zero-param-loop; exploratory (tier X) unless stated.

# Zero-parameter loop: cumulative report, rounds 1-3 plus the round-4 hypothesis merge

Branch `loop/zero-param`, worktree `/mnt/disks/disk-socrateai-local-1/dualscale-wt-loop`. Nothing was pushed or deposited, `proofs/` and `main` were not touched, and LeanMaster was only read. Each number is taken from the file named next to it. Files are relative to `audit/zero_param_loop/`.

## Bottom line

- **Count: 6 -> 2 free parameters** (`mu_sym`, `c4_pta_product`). The goal of 0 is not reached.
- The three accepted steps are an algebraic absorption (round 1), a swap to a literature constant (round 2), and a deletion by exact scaling symmetry, confirmed in code in round 3.
- Neither remaining parameter can be tested with any fetched dataset: fifth-force data and angular PTA data are both ABSENT. Their "freedom" costs nothing in chi2, but it is not derived either.
- The data only support one of the frozen Lean constants. `Omega_m = 1 - omegaLambda` passes (delta chi2 = +0.23). `H0 = c/hubbleRadius_m` fails (delta chi2 = +1204.5, the Hubble tension).
- GUDHI runs correctly and its controls pass, but it has not told models apart in any round.
- **Round-4 proposal, not counted:** set `c4_pta_product = 0`, meaning the PTA correlation is exactly Hellings-Downs (GR). This would give 2 -> 1 if a skeptic accepts it. The number is tier B today. Identifying the model with GR is tier C, and no available Lean statement changes that.

## 1. Parameter ladder

| Round | Free before -> after | What was removed | How | Tier | Skeptic verdict |
|---|---|---|---|---|---|
| 1 | 6 -> 5 | `pta_suppression`, `c4_c0_ratio` -> `c4_pta_product` (nominal 0.08035) | absorbed (the combination still counts as 1) | B | ACCEPTED. Re-splitting the product gives a max diff of 1.1e-16 / 0.0 / 0.0; changing the product moves Gamma by 0.08035 (positive control). Caveat: no data content, and the baseline of "6" was generous. |
| 1 | 5 -> 5 | proposed: `a_pot`, `b_pot` -> `dark_energy_w0 = -1`, `wa = 0` (Observables.lean:97-98) | Lean pin | C | REJECTED. These are Lean definitions, not theorems, and the pinned point is a de Sitter universe with no matter. |
| 1 | 5 -> 5 | proposed: `c4_pta_product` -> `planckGmuBound = 1.5e-7` | Lean pin | L (a bound) | REJECTED. It bounds a different quantity, off by 5.4e5. |
| 2 | 5 -> 3 | `a_pot`, `b_pot` -> flat LCDM with Omega_m = 1 - `omegaLambda` = 0.31115 (DarkEnergyScale.lean:84) | replaced by constant, rule (a). The proposal's label "delete_unobservable" was false. | L constant / C identification | ACCEPTED. Delta chi2 frozen vs fitted = 0.23 < 11.8. The controls Om = 0.28 / 0.35 fail by +10.2 / +6.8. Only 1 dof is frozen; H0 stays a nuisance. |
| 2 | 3 -> 2 | `lambda_sym` | deleted, rule (b): with psi = phi/phi_0, lambda cancels exactly | B | ACCEPTED on condition that the code is rewritten. |
| 3 | 2 -> 2 | `lambda_sym`: condition met (commit 8b8911c; psi-form ODE, parameter gone from signature and `DEFAULT_PARAMS`) | deleted, rule (b) | B (no Lean theorem) | ACCEPTED (skeptic 1d6639f). psi-form vs legacy form: max relative difference 3.8e-5. Cubic-term mutants are caught at 18-74%. Caveat: this holds for the phi/phi_0 ratios only, since lambda sets the absolute fifth-force coupling. The count does not move because lambda_sym was already outside the 2. |
| 3 | 2 -> 2 | proposed: `mu_sym` -> 47 um self-dual length | Lean length bridge | C | REJECTED. There is no bridge formula, and the 1.00017 ratio is circular. `mu_sym` x0.1 moves the screening factor 1.1812e-4 -> 3.2785e-5. |
| 3 | 2 -> 2 | proposed: drop the PTA sector (S3) | scope | X | REJECTED. `c4_pta_product` moves `max_deviation_from_hd` 0.08035 -> 0.8035 / 0.008035. What is missing is data, not a dependence. |
| 4 (proposal) | 2 -> 2 (1 if accepted) | `c4_pta_product` -> 0 (GR: Gamma = Hellings-Downs) | rule (a), pending | number B; no available Lean statement upgrades c4 = 0 to tier A / identification C | NOT YET REVIEWED, not counted. |

**Convention only (does NOT count):** nothing is currently held "fixed by convention". Two items have left this list:
- `lambda_sym` was 0.5-evidence in rounds 1-2 and graduated to "deleted" in round 3.
- `c4_pta_product` was listed here once. The round-3 skeptic reclassified it as fully counted: "This is not convention_only. It moves a retained observable."

## 2. Results per round (real data: DESI 2024 BAO, 12x12 covariance, plus Pantheon+ SH0ES, 1590 SNe, diagonal errors)

Round-1 chi2 values came from a pipeline with two bugs that were later fixed: SN interpolation near z=0, and BAO nuisance clipping. **They are superseded and must not be compared with rounds 2-3.**

| Round | Positive | Negative |
|---|---|---|
| 1 | The absorption was exact. Harness negative controls were added (88f7a51). The GUDHI circle control passed (ratio 62.9). | Superseded pipeline: model 703.88 vs LCDM 697.34, delta AIC 8.54 for LCDM. Response rank 3 at the best fit, but point-dependent ([3,3,4,2,1,2]). Betti [1,0] everywhere. The model-closer-than-null ordering was a bounding-box artifact and was **retracted**. |
| 2 | Frozen LCDM chi2 = 702.907 vs fitted LCDM 702.675 (Om 0.3170). It beats the quintessence sweep's own minimum of 703.895 with 0 shape parameters (`round2/decisive_experiment_report.json`). The negative control Om_L = 0.5 gives 856.15. The round-1 "duplicate bottleneck" was explained: it is half the dominant bar's persistence, not a bug. | Response rank 3. Betti: model [2,0], real [1,0]. The TDA nulls are not comparable across the two embeddings, so no topology claim is made. The SN chi2/N of 0.43 means absolute thresholds are uncalibrated. |
| 3 | The chi2 re-implemented independently reproduces 702.906 / 702.675 (`round3/skeptic/chi2_independent.json`). CPL with Om frozen gives w0 = -0.8436, wa = -0.4863, chi2 lower by 6.29 (below 11.8; mild). Response rank 2 at nominal, as predicted; SVs 0.788 and 0.0021 (`round3/jacobian_report.json`). The GUDHI circle control passed (49.05). The skeptic's real-resubsample vs Poisson null separates on H1 (`round3/skeptic/tda_skeptic_report.json`). This tests real data against the null, not the model against the data. | **Frozen H0 = 67.661 is REJECTED**: delta chi2 = +1204.48, while the fitted offset implies H0 = 73.43. chi2 is flat at 702.906608 across the whole (mu_sym, c4) sweep, so neither parameter can be tested. delta AIC = +4.00 / delta BIC = +14.76 vs frozen LCDM; this is **definitional** (2 untested parameters), not a measured effect. Betti: model [2,0] vs predicted [1,0] at seeds 42 and 43 (a miss; a sparse-Rips artifact). The PCA dimension of 2 matches the prediction (a hit), and these are two separate facts. The null-vs-real H1 value of 0.0325 triggers the half-max-persistence degeneracy, so model 0.0061 < null carries no evidence. The skeptic also found and fixed a regression: `KeyError: 'dphi_dr'` in `workshopcosmo.py:1517`. |
| 4 (probe written for this report) | Self-dual vacuum variant A gives chi2 698.195, delta -4.71 vs frozen LCDM. That recovers 75% of the CPL gain with 0 fitted shape parameters. The Hellings-Downs Legendre coefficients match the closed form to 4e-17, with a_4 = 3/80 and the known-answer check a_0 = a_1 = 0 passing (`round4/adaptation_probe.json`). | A was chosen after the fact, out of 4 variants: the mirror B scores +9.17, C +5.07, D +0.90. A's gain is below 11.8. The H0 implied by A is 73.11, so it does nothing for the tension. |

Command for round 4: `.venv-tda/bin/python audit/zero_param_loop/round4/adaptation_probe.py` writes `round4/adaptation_probe.json`.

## 3. What the reverse loop says, and the adapted hypothesis

What the reverse loop says:
- The dark-energy data want flat LCDM with Omega_m near Planck. The toy quintessence potential adds freedom that the data do not reward.
- The one sharp zero-parameter claim the data can check, H0 from `hubbleRadius_m`, fails. The reverse agent's round-3 phrase "dark energy is indistinguishably the frozen Lean constant (Omega_Lambda, H0)" is therefore refuted for H0.
- The constants are imported from Planck 2018, not derived. CKNInstance.lean `planckEnergy_gt_ckn_horizon_cutoff` falsifies the literal l_micro ~ l_P pairing at 10^30.
- LeanMaster's zero-parameter K3xT2 results (dyon counts, chi(K3) = 24) touch no observable in this harness.

**Adopted: a frozen-vacuum LCDM background, a GR tensor sector and one screening scale** (merge of the MINIMAL, DERIVE and DATA-FIRST angles):
1. **Background:** flat LCDM with Omega_m = 0.31115, from MINIMAL. This has been in place since round 2. H0 and the SN absolute scale remain nuisances, as disclosed.
2. **PTA:** Gamma(theta) = Hellings-Downs exactly, i.e. `c4_pta_product = 0`, from DERIVE target A. The model's total l=4 coefficient becomes 3/80 = 0.0375 instead of 0.11785 at nominal. This makes a falsifiable prediction once angular data exist. The cost is that the PTA sector then has no content beyond GR (C).
3. **Screening:** `mu_sym` stays free and counts as 1. The TriadContract bound (<= 124 ppm) could give an interval certificate, but not a value.

Ideas dropped, and why:

| Idea | Why dropped |
|---|---|
| Delete the screening and PTA sectors by scope | This shrinks the observables, not the parameters. The round-3 skeptic already rejected it. |
| Freeze H0 = c/hubbleRadius_m | Rejected by the data (+1204.48). |
| Self-dual vacuum, variant A (DATA-FIRST 1) | Chosen post hoc from 4 variants (look-elsewhere effect), delta -4.71 < 11.8, and it touches no counted parameter. Kept only as a pre-registered test for independent data. |
| Symmetron symmetry-breaking epoch at a* | It fixes mu*M, not the dimensionless `mu_sym`. Since M is free, nothing is absorbed. |
| `mu_sym` from the 47 um length | There is no unit bridge, and the ratio is circular. The ~1.6x tension with Lee et al. 2020 is LeanMaster's docstring claim and was not verified here. |
| `c4` from moonshine ratios or `planckGmuBound` | No Lean link exists, so this would be numerology. `planckGmuBound` bounds a different quantity. |
| TDA as a discriminator | Betti [2,0] is an artifact, and the null distance is degenerate. |

## 4. Next steps (one each)

**Decisive experiment: an angular PTA test of `c4_pta_product = 0`.**
- Method: fit Gamma(theta) = HD + c4 * P4(cos theta) to a real binned pulsar-pair cross-correlation product (NANOGrav 15-yr optimal statistic).
- Pre-registered rule: reject c4 = 0 if delta chi2 > 9 (1 dof, 3 sigma). Report on the nominal 0.08035 with the same rule.
- Negative control: a monopole-only curve must be rejected, otherwise the data have no angular power and the test is void.
- **Status: ABSENT-blocked.** No angular product is in `data/real`; the ceffyl zip is a per-frequency KDE. The statistical power for an l=4 excess is unknown (ABSENT).

**Lean statement to prove (in LeanMaster, currently ABSENT there):**

```lean
noncomputable def hdCurve (t : ℝ) : ℝ :=
  3/2 * ((1 - t)/2) * Real.log ((1 - t)/2) - 1/4 * ((1 - t)/2) + 1/2
theorem hd_legendre_coeff_four :
    (9/2 : ℝ) * ∫ t in (-1:ℝ)..1, hdCurve t * ((35 * t^4 - 30 * t^2 + 3) / 8) = 3/80
```

- Current evidence is tier B: quadrature gives 0.03750000000000004.
- Once kernel-checked, the theorem is tier A, but it only fixes a number. The claim that the dual-scale PTA sector has no l=4 excess stays tier C.
- The gap, stated plainly: `c4_pta_product` is an additive excess on top of `hd_curve` (`scripts/param_loop_sim.py:236`). This theorem takes `hd_curve` as a definition and integrates it, so it fixes GR's own l=4 number exactly; it does not derive the model's. Deriving `c4 = 0` would need the massless transverse-traceless overlap integral (the graviton polarization sum), which is ABSENT in LeanMaster and is more than a one-theorem job. **No Lean statement currently within reach removes a counted parameter.**
- The round-1 target `symmetron_profile_lambda_indep` is now superseded: it would upgrade lambda_sym from B to A but no longer moves the count.

## 5. ABSENT, fabricated or overturned (verbatim where quoted)

ABSENT
- `pta_chi2`: "ABSENT: NANOGrav verified product (ceffyl_data/*.npy) is a per-frequency free-spectrum KDE, no angular/pulsar-pair separation info; no real Gamma(theta) dataset exists to compare against."
- `fifth_force_screening`: "Reason: No widely-available public machine-readable dataset for fifth force or screening mechanisms (Chameleon, Symmetron, f(R) gravity). Such data typically appears as figures in theoretical papers rather than released data tables. Searched: arXiv, GitHub physics orgs, Zenodo. Conclusion: Fifth force parameter space is derived from numerical simulations or fits to literature bounds, not open observational/simulation data."
- Pantheon+ full STAT+SYS covariance: not in `data/real/dark_energy`. Every SN chi2 uses diagonal errors.
- "I did not open those three files, so whether they contain a usable screening ODE, quintessence dynamics or PTA response is ABSENT." (DualScaleM24Formalization.lean, UseCase3_FrontierTriad.lean, TriadContract.lean)
- The 124 ppm Bertotti-Iess-Tortora figure: "I have not checked it against that paper (ABSENT)."
- BAO-only frozen test with r_d: "Status: ABSENT, not computed."
- DESI DR2, Pantheon+ full covariance, Union3/DES-Y5 for the self-dual-vacuum test: "All are ABSENT locally."
- Torsion-balance table: "That table is ABSENT, and I have no verified bound to cite."
- A Hellings-Downs / Legendre statement in LeanMaster: ABSENT (grep at HEAD 5c0423b).
- Resolved since the last report: the SH0ES frozen-offset variant (was "no M_B fiducial") was computed in round 3 and is REJECTED (+1204.48).

Fabricated: **none.** No skeptic identified a fabricated number or dataset, and `data_rejected` = [].

Overturned or corrected after review (these are not fabrications):
- The round-1 claim "Lean w0/wa measured and actively disfavored by data" was an invalid inference, because the pinned point has no matter.
- The round-1 TDA model-closer-than-null ordering was a bounding-box artifact and was retracted.
- The round-2 label "delete_unobservable" for a_pot/b_pot was false and has been relabelled rule (a).
- The round-3 47 um ratio 1.00017 was circular (47309d6). It had also been mislabelled a "short-range gravity bound".
- Round-3 reverse: a self-comparing probe was first described as an "independent refit agreement" (10d3ed2).
- Round-3 reverse: "H0 from hubbleRadius_m" as indistinguishable was refuted (+1204.48).
- Commit 8b8911c claimed "94 passed, 6 skipped"; the skeptic measured 98 passed, 2 skipped. The same commit broke `workshopcosmo.py main()`; the skeptic fixed it in 1d6639f and disclosed that it had edited code under review.
- The harness selftest had no coverage for mu_sym, lambda_sym or the PTA parameters before 88f7a51.

Known broken and not used as evidence: `vacuum_decay_cdl` (floored action), the `tadpole_cancellation_certified` flag, and local `proofs/BuscherRules.lean`.

Tier note: the constants table labels `omegaLambda` "Tier A". As a Lean `def` the number is kernel-fixed. As physics it is tier L (Planck 2018), and identifying it with the model's parameter is tier C.
