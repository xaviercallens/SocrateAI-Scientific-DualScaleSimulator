Generated by workflow reverse-to-zero-v2; exploratory (tier X) unless stated; M0 is a hypothesis change, not a derivation.

# Reverse-to-zero, round 2 (reverse_zero_r2)

Worktree `/mnt/disks/disk-socrateai-local-1/dualscale-wt-reverse`, branch `loop/reverse-zero`. Registration frozen at commit `9e6705e940f35aef8b377ec3631f62ba3d52aea4` before any data load; experiments at `55f32dd` (X4), `9cefc3e`+`3722b65` (X2), `0ce246c`+`d78d27a` (X3), `7bc2688` (X1); skeptics at `aeb944f` (framing) and `2b399aa`/`62a09e0` (statistics); ledger at `3c10310`. Nothing pushed (branch is 11 commits ahead of `origin/loop/reverse-zero`, still at `8e90996`). `proofs/`, `main`, other worktrees, LeanMaster and `PRE_REGISTRATION.md` were not touched — all read-only this round.

## 1. Bottom line

**M0** carries **zero free dark-sector parameters by hypothesis change**: Ω_Λ is frozen at the imported Planck18 value 0.68885 (tier L, not derived), the symmetron sector is removed, and the GR tensor sector is fixed (c4_pta_product = 0). It is **not** parameter-free in the fitting sense used by X4: it carries free `h` and `ω_b` plus two profiled nuisances (`r_d·h`, the SN offset), and H0 is explicitly **not** frozen (PRE_REGISTRATION R1 — freezing H0 against SH0ES-calibrated SNe was rejected at Δχ²=+1204.5 in the zero-param-loop line of work). **M2** (the reduced dual-scale cosmology carried over from that same line of work) has **2** free parameters, unchanged this round — no X1–X4 experiment re-derives or re-counts M2's parameter list.

**c4_pta_product did NOT move from untested to bounded.** X3's registered decision rule (verbatim, §2 below) calls for a bound, `'no bound'`, or `ABSENT`. Two amplitude choices were tried, neither independently verified against a NANOGrav source:
- at log10_A_CURN = −14.62: the registered covariance gate passes (mean χ²/7 = 0.931, in [0.8, 1.2]) and the 95% Fieller interval is **[−0.841, 0.101]** — it contains 0, so M0 (c4=0) is "consistent with" the data *at this amplitude*;
- at log10_A_CURN = −14.0 (a full independent rerun, everything else identical): the gate **fails** (mean χ²/7 = 0.424), and per the registered rule ("otherwise the covariance is flagged and no bound is reported") the correct report there is **no bound**.

c4_pta_product is therefore not invariant to this unverified nuisance amplitude, and the statistics skeptic independently marks the registered X3 test as **not met as specified** (`stands=false`): the registration names the Allen-2023 pair-covariance formalism, and a delete-one-pulsar jackknife was substituted (confirmed absent from installed `enterprise_extensions` by the experiment's own grep); `hd_recovery` was computed as the joint 2-parameter GLS `a_hat` rather than the registered `c4`-fixed-at-0 single-parameter refit; the CURN amplitude itself was never fetched/verified. **Correct reading: c4_pta_product remains an untested-in-the-registered-sense parameter with one conditional, non-robust interval reported for context, not a bound.** PRE_REGISTRATION P3's row for it (line 65, "bound it") is unchanged.

## 2. Registered tests X1–X4

Quoted `decision_rule` text below is copied directly from `registration.json`'s `tests` dict at commit `9e6705e` (`git show 9e6705e:audit/reverse_zero_r2/registration/registration.json`), not from any narrative summary — the framing skeptic caught exactly this failure mode (a spliced, non-verbatim "decision rule" attributed to X3, finding V1 below), so this section re-derives each quote from the frozen file itself. **Registration commit `9e6705e` is confirmed (`git merge-base --is-ancestor`) an ancestor of every data-load commit: `7bc2688` (X1), `9cefc3e` (X2), `d78d27a` (X3), `55f32dd` (X4).** Deviations are listed first for each test, per the ground rules.

### X1 — CMB TDA, spectrum-matched null (WMAP primary, SMICA conditional)
**Deviations:** null-sim count 500 per map, not the registered "up to 2000" (p-value resolution 1/501 vs 1/2001), under the per-command memory/time cap and heavy shared-machine CPU contention (load average 18–27 on 8 cores observed). No A=0 control row in the string-injection table, so the ~10% detection rate at the smallest amplitude cannot be separated from the test's own false-positive rate.

Registered `decision_rule` (verbatim, three branches):
> `if_gate_fails`: "No p-values. M0 outcome: NOT TESTED."
> `if_p_corr_ge_0.05`: "Gaussian null NOT rejected: M0's Gaussian prediction is consistent with the map at the sensitivity established by the string power table. Not evidence for M0 over alternatives; no K3xT2 statement."
> `if_p_corr_lt_0.05`: "The spectrum-matched Gaussian null is rejected by that statistic for that map and pipeline. Causes not separable: non-Gaussianity, foreground/ILC residual, mask leakage, or a null-model defect. Reported as an anomaly, not as a failure of M0 and not as support for any alternative. A rejection in WMAP but not in SMICA (or vice versa) is reported as such."

**Outcome under the rule:** WMAP (primary family): gate passed (max|z|=0.851 ≤ 2.807), p_corr (Sidak, N_eff=2) = 0.472 ≥ 0.05 → Gaussian null **not rejected**. SMICA (separate, conditional family per registration, no cross-map pooling): gate passed (max|z|=0.166), p_corr = 0.004 < 0.05 → **anomaly**, causes not separable (chance, foregrounds/point sources, mask-edge effects, component-separation residuals candidates; none isolated). Exactly the registered branch's third clause fires: "a rejection in WMAP but not in SMICA... reported as such." No K3×T2 statement in either branch.

### X2 — Cosmic-web TDA, redshift-space, powered gate
**Deviations:** none confirmed as real code deviations. `X2-cosmic-web/REPORT.md` claims a seed deviation (used 20260919 instead of the registered 20260920); independently re-checked this round by reading `x2_lib.py` directly — `SEEDS["randoms"] = 20260920`, the registered value. **REPORT.md's self-described deviation is a documentation error, not an actual deviation** (skeptic finding V5, confirmed).

Registered `decision_rule` (verbatim):
> `gate_fail`: "no topology verdict (INCONCLUSIVE); reason recorded"
> `gate_pass_and_p_corr_ge_0.05`: "not rejected: the data topology at R_s = 20 Mpc/h is consistent with the lognormal redshift-space null (which encodes Gaussian initial conditions with linear bias, i.e. M0's frozen-LCDM assumptions)."
> `gate_pass_and_p_corr_lt_0.05`: "the null is rejected at R_s = 20 Mpc/h. This is a statement about the mock model (lognormal + linear bias + FoG) at least as much as about M0; no attribution to K3xT2 or to any alternative."
> `note`: "any excess at r < 20 Mpc/h is outside the validated range and cannot enter a verdict"

**Outcome under the rule:** power was demonstrated first (Poisson-null rejection 0.995, ×3-amplitude rejection 0.985, both ≥0.95). The calibration gate then **fails**: data χ²(G) = 40.215 vs. the 500-mock 95th-percentile threshold 20.176 (two-sided empirical p = 0.00399, 2.879σ via `scipy.stats.chi2.sf` then `norm.isf(p/2)`). Per `gate_fail`, the registered verdict is **INCONCLUSIVE** — no topology p-value is part of it. An informational-only, explicitly-labeled deviation computed the topology p_corr anyway (0.341, Sidak N_eff=3) for context; it is not the verdict. Void/ring controls (1.00 / 0.033 detection) show the pipeline has real power for evacuated regions but is information-starved for filament-scale topology at this survey volume.

### X3 — NANOGrav 15-yr PTA, c4_pta_product
**Deviations (three registered elements not met, per the statistics skeptic):** the registered Allen-2023 analytic pair-covariance formalism was replaced by a delete-one-pulsar jackknife (`enterprise_extensions` 3.0.3 has no such function, confirmed by grep); `hd_recovery` was computed as the joint 2-parameter GLS `a_hat` rather than the registered `c4=0`-fixed single-parameter refit; the CURN amplitude the whole fit is conditioned on was never fetched/verified against a NANOGrav source. J1713+0747 is excluded (PINT `Pulsar()` construction did not complete in 3 foreground attempts of ~10 min each, a computational not data-quality exclusion; direction of its effect on c4 unknown).

Registered `decision_rule` (verbatim): "Report |c4| < x at 95% (x from the Fieller interval) as a bound, or 'no bound', or ABSENT. M0 (c4 = 0) is 'consistent with' the data iff 0 lies inside the interval. No statement that M0 or K3xT2 predicts c4." Registered `calibration_gates.covariance_check` (verbatim, a *separate* field, quoted separately per the ground rules rather than spliced with the above): "1000 sky scrambles (random permutations of pulsar sky positions, seeds 8000000 + k) run through the same estimator and binning: mean of chi2(rho_bin, C)/7 must lie in [0.8, 1.2]; otherwise the covariance is flagged and no bound is reported."

**Outcome under the rule:** at log10_A_CURN=−14.62 (66 pulsars, 2145 pairs, 7 bins): gate mean χ²/7 = 0.931 (in range) → Fieller interval reported: **[−0.841, 0.101]** at 95%, contains 0 → "consistent with" c4=0, *conditional on this unverified amplitude*. At log10_A_CURN=−14.0: gate mean χ²/7 = 0.424 (outside [0.8,1.2]) → per the covariance_check clause, **no bound is reported**. Because the registered covariance formalism itself was substituted, the statistics skeptic's checklist marks this test `stands=false` even though the CONDITIONAL framing in the writeup is judged correct given the substitution.

### X4 — DESI DR2 BAO + CMB distance priors + Pantheon+
**Deviations:** `r_drag(ω_b,ω_m)` supplied via a CAMB-2.0.4-built interpolation table rather than a hand literature fitting formula (validated to 1.4e-5 relative error — a strengthening, disclosed as a deviation from the registered spec's wording). One of 20 seeded CPL starts (index 3, both SN cuts) converges to a distinct higher local minimum, excluded from convergence counts (19/20 agree to <1e-3).

Registered decision content (verbatim from `registration.json` X4, `status_for_P1` / `reporting` / `for_M0` fields — quoted as separate named fields, not merged): `reporting`: "X4 reports Delta chi2, the p-value and sigma for each comparison, primary (1580) and disclosed (1590) cuts, and says 'DR2, informational for PRE_REGISTRATION P1; not a P1 verdict'. Words such as 'falsified' or 'confirmed' are not used." `for_M0`: "M0 vs Lambda: Delta chi2 with 1 dof; M0 'not rejected' if p >= 0.05, else 'disfavoured relative to fitted Lambda at that DR2 combination', still no P1 verdict." (Note: the field says "at that DR2 combination" — **not** "at that DR2 + compressed-CMB combination"; the latter, longer phrase in `X4_RESULT.md:138` labeled "registration's own phrasing" is not a verbatim match — skeptic finding V3, confirmed by `grep -F` failing.)

**Outcome:** CPL vs. Λ (2 dof, primary): Δχ²=6.999, p=0.0302, **2.167σ** (1580-SNe cut); 2.121σ (1590 cut). M0 vs. fitted Λ (1 dof, reported alongside, not a rejection test): Δχ²=4.675, **2.162σ**, p=0.0306 — up from round 1's 0.86σ (BAO+SN only, no CMB) because the CMB distance priors break the Ω_m–h degeneracy. Both are **INFORMATIONAL for PRE_REGISTRATION P1** — PRE_REGISTRATION.md line 41 requires "DESI DR3 or final BAO combined with CMB and any one major SN compilation" (verbatim, `grep -F` confirmed); DR2 is not DR3/final, so no P1 verdict follows (ground rule 10). The published R4 3.1σ is **not approached** (shortfall ≈0.93–0.98σ), expected given compressed distance priors rather than the full Planck likelihood — stated as expected, not anomalous.

## 3. What changed relative to round 1 (REPORT `13756dc`, ERRATA `b77d11a`)

- **X1 vs. round-1 E5 (CMB TDA):** round 1's null pseudo-Cℓ did not match the data (z up to ±11.6 across ℓ-bands) and its recalibration failed its own gate, so "M0's Gaussian prediction survives" was an unsupported claim (ERRATA). Round 2 fixed the three identified pseudo-Cℓ defects — `lmax` now 3×nside=384 (was 2×nside=256), `pixwin` off consistently for data and every sim, mask applied exactly once through one shared code path — and the WMAP spectrum-match gate now passes cleanly (max|z|=0.851 vs. threshold 2.807). Round 2 also newly fetched and ran Planck PR3 SMICA as an independent, conditional family, which round 1 did not have; it surfaces a genuine anomaly (p_corr=0.004) that WMAP does not show.
- **X2 vs. round-1 E4/E5b (cosmic-web TDA):** round 1's gate had no statistical power (`ξ=0` passed it) and its Poisson-null comoving-distance shells were mismatched (KS p=0.009), so its INCONCLUSIVE verdict rested on an untested gate. Round 2 built and demonstrated gate power first (0.995/0.985 rejection rates, ≥0.95 required) and fixed the shell-matching (KS median 0.43/0.61). The round-2 INCONCLUSIVE verdict is a *different, stronger* finding: the (now-powered) gate is **failing**, meaning the lognormal+linear-bias+FoG mock family itself does not reproduce the data's clustering shape on G=[20,60) Mpc/h — confirmed by a post-hoc diagnostic showing this persists even at the G-range's own best-fit bias (χ²=33.9 still above the 20.18 threshold).
- **X3 vs. round-1 E2 (PTA):** round 1 never computed a bound at all — its own writeup incorrectly claimed LeanMaster "confirms mu_sym and c4_pta_product are unchanged" (ERRATA-corrected to "untested, a project statement, not LeanMaster's"). Round 2 is the first attempt at an actual numeric interval, using a newly installed `enterprise`/PINT stack; the result is conditional rather than a clean bound, but it is qualitatively new information, not a restatement of "untested."
- **X4 vs. round-1 E1+E3 (DESI DR2 BAO+SN, M0):** round 1 ran BAO+SN with no CMB (Δχ²=4.547, 1.63σ CPL-vs-Λ; M0-vs-Λ 0.86σ) and used two slightly different SN cuts across E1/E3, corrected in the ERRATA table ("Different cuts... verdict unchanged"). Round 2 unifies both into one script with one SN cut and adds a newly fetched-and-verified CMB distance-prior table (Chen, Huang & Wang 1808.05724, cross-checked against a CAMB recomputation), raising CPL-vs-Λ to 2.12–2.17σ and M0-vs-Λ from 0.86σ to 2.16–2.17σ (still not 3σ) — a genuinely new, better-constrained number rather than a reproduction.
- **Process change:** round 2 introduces a commit-frozen `registration.json` (`9e6705e`) written before any data file was opened (`no_data_loaded_statement` field verified: "produced from file names, os.stat sizes, and text-file quote checks only"), which round 1 did not have. Round 1's quote-fabrication defect (invented LeanMaster text) does not recur in the machine-generated `quotes_verbatim` JSON blocks this round; it recurs instead in free-form Markdown prose (V1–V4 below) — the same failure mode, one layer up, exactly where a defect of this class would still be possible.

## 4. LeanMaster at HEAD

The task's supplied `LEANMASTER` block claims HEAD = `eb791e7` with "No commits exist after eb791e7." This is **stale**. Independently re-verified this round (`git rev-parse HEAD` / `git describe --tags` in `/home/callensxavier_gmail_com/SocrateAI-Scientific-Agora-LeanMaster`): true HEAD is **`64f905ffc63c07b4d7ec4b3a7a62bc975239f60d` (v3.28.0)**, 4 commits ahead:
```
64f905f LeanMaster as an MCP server for other Claude sessions and projects; README §8 rewritten on leanstack; docstring fix
225abc5 Stream 8 open question 2 answered (orbifold weights); gate tools cover @[simp] theorems; leanstack (LeanCache/LeanMemory/LeanDatastore/LeanGraph/LeanRAG)
404f4b9 LL §S8.5: spell out why standard U2(Z[w]) is monomial; the 96/72 counts stand
1ff4e25 Stream 8 P8.4c: trapping on the K3 factor selects SO(40); globally SO(44), not a product point; LL §S8
```
**What these close:** Stream 8's open question 2 (orbifold weights) is answered; P8.4c resolves the K3-factor trapping question to SO(40) locally / SO(44) globally (not a product point).

A **full-tree** `git grep` at true HEAD `64f905f` (strictly stronger than `git show <one-commit> | grep`, which searches only that single commit's diff, and is what the task's own supplied LEANMASTER block used) finds:
- `mu_sym`: no match.
- `c4_pta_product`: no match.
- `unit-bearing` / `TDA prediction`: no match.
- `Observables: none` (verbatim): **found**, 3 sites, all in `docs/STREAM8_WHICH_K3.md` (lines 115, 156, 377).

**What this closes:** the four new commits do not touch Stream 8's observable-scope statement. "Observables: none" still holds at true HEAD, so no new LeanMaster evidence bears on mu_sym, c4_pta_product, Ω_Λ or any TDA outcome this round; ground rule 5's prohibition (never write that K3×T2 predicts or fixes these) is unaffected by the HEAD advance.

## 5. PROPOSED ADDENDUM text for PRE_REGISTRATION.md

*(Not written to the file — `audit/PRE_REGISTRATION.md` is read-only per the ground rules. Existing addenda run A1–A5 (lines 89–97), so the next available letter is **A6**, appended, never editing A1–A5 or the earlier body.)*

> **A6. Reverse-to-zero round 2 (2026-09-19): X1–X4 registered and run, worktree `dualscale-wt-reverse`, registration `audit/reverse_zero_r2/registration/registration.json` frozen at commit 9e6705e before any data load.**
> - **X1 (CMB TDA, spectrum-matched null).** WMAP ILC (primary): Gaussian isotropic null **not rejected** (p_corr=0.472, Sidak N_eff=2). Planck PR3 SMICA (conditional, no pooling): **anomaly**, p_corr=0.004, causes not separable (chance / foregrounds / mask-edge / component-separation residuals candidates, none isolated). No K3×T2 statement either way.
> - **X2 (cosmic-web TDA, redshift space, powered gate).** Gate power demonstrated (≥0.995/0.985 rejection, required ≥0.95). Calibration **fails** (data χ²(G)=40.21 vs. threshold 20.18, p=0.004, 2.88σ): **INCONCLUSIVE** per the registered rule — a statement about the lognormal+linear-bias+FoG mock family, not a P1 or M0 verdict.
> - **X3 (NANOGrav 15-yr, c4_pta_product).** **No new bound established.** Conditional Fieller interval [−0.841, 0.101] at one unverified CURN amplitude (log10_A_CURN=−14.62); no bound at an alternate untested amplitude (−14.0, gate fails). PRE_REGISTRATION P3's c4_pta_product row is unchanged.
> - **X4 (DESI DR2 BAO + CMB distance priors + Pantheon+).** **INFORMATIONAL for P1** (DR2 is not DR3/final BAO+CMB+SN, Euclid, or Rubin; P1's rule does not apply). CPL-vs-Λ: 2.17σ (1580 SNe) / 2.12σ (1590 SNe) — informative but short of the published R4 3.1σ (compressed distance priors, not the full Planck likelihood). M0-vs-fitted-Λ (reported alongside, not a rejection test): 2.16–2.17σ, up from A3's 0.86σ once CMB priors are added.
> - Full detail, seeds, deviations and skeptic review: `audit/reverse_zero_r2/REPORT.md` and `ledger_reverse_zero_r2.json` (commit 3c10310, branch `loop/reverse-zero`, not merged to main).

## 6. Ranked next steps (Tier C, exploratory)

1. **Install `enterprise`'s Allen-2023 covariance path (or implement it directly) for X3, and fetch/verify the actual NANOGrav 15-yr CURN amplitude against the published paper.** Observable: c4_pta_product (dimensionless), 95% Fieller interval width. Freeze-before-compare: fix the covariance formalism and the CURN amplitude source *before* rerunning the GLS fit on the same 66-pulsar cache. Difficulty: moderate–hard (real analytic-covariance implementation, or a working `enterprise` full-PTA run; J1713+0747's PINT failure suggests real engineering cost).
2. **Diagnose the X2 gate failure's shape mismatch** (best-fit b(G)=1.2 still gives χ²=33.9 > 20.18): is it the lognormal transform, the CAMB linear P(k) normalization, or FoG at R_s=20? Observable: χ²(ξ(r), G=[20,60) Mpc/h) (dimensionless), 10×10 Hartlap-corrected. Freeze-before-compare: pick one candidate mock-model change, register its exact form, rerun once. Difficulty: moderate (needs a fresh, large mock ensemble each iteration; memory/time-capped on this machine).
3. **Rerun X1 at the registered N=2000 null sims** (currently 500) for both WMAP and SMICA, to sharpen the SMICA p_corr=0.004 anomaly's resolution and rule the ~10% low-amplitude string-injection detection rate in or out as a false-positive floor (an A=0 control row is also needed). Observable: p_corr (dimensionless), string-injection A/σ (dimensionless). Freeze-before-compare: same seeds, same code path, just more N. Difficulty: easy in method, hard only in wall-clock/CPU-contention budget.
4. **Full Planck plik-lite (or equivalent) CMB likelihood for X4** in place of the 3-number compressed distance-prior approximation, to see whether CPL-vs-Λ actually reaches the published 3.1σ. Observable: Δχ²(CPL, Λ) (dimensionless), σ via Wilks. Freeze-before-compare: register the exact likelihood file and its nuisance treatment before loading it. Difficulty: hard (full likelihood code, nuisance parameters, foreground marginalization).
5. **Reconstruct J1713+0747** with a longer PINT budget or a lighter-weight per-pulsar model, and check whether including this high-precision pulsar shifts a_hat's 2.42σ HD detection or the X3 interval's endpoints. Observable: a_hat SNR (σ), c4_pta_product interval bounds (dimensionless). Freeze-before-compare: fix the exact noise model and TOA cuts before adding the pulsar back in. Difficulty: hard (already failed 3 timed attempts this round).

## 7. ERRATA table for this round

| # | Where (file:line) | As committed | Correct reading |
|---|---|---|---|
| V1 | `audit/reverse_zero_r2/X3-pta/README.md:117` | `"Per the registered decision rule (\"Gate fails: no p-values / no bound is reported\")"` attributed to X3's registered decision rule | Spliced composite: "Gate fails: no p-values" paraphrases X1's `decision_rule.if_gate_fails` (a **different test**); "no bound is reported" is a genuine fragment of X3's `calibration_gates.covariance_check`, not of the field actually named `decision_rule`. `grep -F` for the quoted string against `registration.json` fails. Does not change X3's CONDITIONAL/no-unconditional-bound verdict. |
| V2 | `audit/reverse_zero_r2/X3-pta/README.md:45` | `"14 frequencies, gamma=13/3, fixed noise"` labeled as registration wording | Not a verbatim substring; the real field (`tests.X3.statistic.estimator`) reads "...with fixed per-pulsar noise from the release, spectrum gamma = 13/3, first 14 frequencies 1/T..14/T..." — different word order and spacing. Does not change the numeric result. |
| V3 | `audit/reverse_zero_r2/X4-bao-cmb-sn/X4_RESULT.md:138` | `"disfavoured relative to fitted Lambda at that DR2 + compressed-CMB combination"` labeled "registration's own phrasing" | Registration's actual text (`tests.X4.for_M0`) reads "at that DR2 combination" — no "+ compressed-CMB" clause. Invented addition inside quotation marks; `grep -F` fails. Correct M0-vs-Λ sigma (2.16–2.17σ) is unaffected. |
| V4 | `audit/reverse_zero_r2/X3-pta/README.md:90` | `"chi2/7"` called "the registered wording" | Non-verbatim shorthand of the actual field text `"chi2(rho_bin, C)/7"`. Low severity; no numeric effect. |
| V5 | `audit/reverse_zero_r2/X2-cosmic-web/REPORT.md:133` | Claims randoms seed 20260919 was used (a deviation from the registered 20260920) | Independently re-checked this round: `x2_lib.py` line 36 sets `SEEDS["randoms"] = 20260920`, the **registered** value. REPORT.md's self-described deviation is a documentation error, not an actual code deviation. INCONCLUSIVE verdict is unaffected either way (the calibration gate fails on the χ² statistic, unrelated to the randoms seed choice). |
| V6 | `audit/reverse_zero_r2/X1-cmb/smica/result.json` (via the round-2 EXPERIMENTS narrative's `var_ratio_sim_over_data` causal claim) | SMICA's null-sim pixel-variance deficit (var_ratio=0.981) offered as a "candidate partial cause" of the p_corr=0.004 anomaly | Unsupported by the script's own normalization: `four_curves` (in `x1_cmb.py`) divides every map by its own standard deviation *before* thresholding, so a per-map variance ratio cannot mechanically drive the statistic. Does not change p_corr=0.004 or the registered "anomaly, causes not separable" verdict — it removes one candidate mechanism from the (still unresolved) list. |

## 8. ABSENT, NOT ATTEMPTED, disputed, or from memory

**ABSENT (fetch/implementation genuinely missing):**
- X3's registered Allen-2023 analytic pair-covariance formalism — `enterprise_extensions` 3.0.3 has no such function (confirmed by grep of its source); a jackknife was substituted instead.
- X4's full Planck power-spectrum likelihood — only 3 compressed distance-prior numbers (R, l_A, ω_b) were used, disclosed as understating the constraining power relative to the published DR2+CMB+SN 3.1σ result.
- Full-N (193,536-galaxy) direct pair-count cross-check of X2's grid ξ(r) estimator at G-range precision — only an n=6000 subsample was checked.

**NOT ATTEMPTED (explicitly declined this round, with reason):**
- Regenerating all 1130 X2 mock catalogues to test with the registered randoms seed — moot per V5 above (the code already uses the registered seed; nothing needed regenerating).
- Fetching/verifying the exact NANOGrav 15-yr γ=13/3 CURN amplitude against a NANOGrav paper or table.
- A second, independent CAMB σ8 self-check for X2 analogous to round 1's top-hat-quadrature check.
- Recovering J1713+0747 with a different noise model (3 foreground PINT attempts of ~10 min each were made and failed; a 4th was not attempted this round).

**NOT ABSENT — do not re-list as such:** Planck PR3 SMICA + common mask **were successfully fetched** this round (Planck Legacy Archive AIO endpoint, attempt 2 of 3 allowed, after two IRSA URLs 404'd), sha256-recorded; and the CMB distance-prior table for X4 **was fetched and verified** (arXiv:1808.05724, Chen/Huang/Wang), cross-checked against a fresh CAMB recomputation.

**Disputed (genuine cross-lens disagreement, recorded rather than averaged):** X3's registered-test compliance. The framing/claims-and-quotes lens finds no verdict-changing violation in X3's numeric result (its V1/V2/V4 quote defects are citation-accuracy problems, not computation problems). The statistics/reproducibility lens marks the registered X3 test `stands=false` because the substituted covariance, the joint-fit `hd_recovery`, and the unverified CURN amplitude mean the test as specified was not the test performed. Both are correct under their own checklist items; §1's "CONDITIONAL, no unconditional bound" framing is consistent with both.

**From memory (then verified, the clean case):** the Chen, Huang & Wang CMB distance-prior citation (arXiv:1808.05724) started as a recalled arXiv id at registration time (registration.json's `statistic.covariance` field for X3 similarly flags "the arXiv id is to be verified... at fetch time, not asserted here" as the general pattern) and was subsequently fetched and its table/inverse-covariance grep-matched verbatim against the TeX source this round — the intended discipline working as designed.

---

**Artifacts committed this round** (all under `/mnt/disks/disk-socrateai-local-1/dualscale-wt-reverse/audit/reverse_zero_r2/`, branch `loop/reverse-zero`, not pushed): `registration/registration.json` (9e6705e); `X1-cmb/` (7bc2688); `X2-cosmic-web/` (9cefc3e, 3722b65); `X3-pta/` (0ce246c, d78d27a); `X4-bao-cmb-sn/` (55f32dd); `skeptic_framing/verdict.json` (aeb944f); `skeptic_statistics/verdict.json` (2b399aa, 62a09e0); `build_ledger_r2.py` and `ledger_reverse_zero_r2.json` (3c10310) — the ledger is built deterministically by reading each input via `git show <pinned-commit>:<path>` and reproduces byte-identically across reruns.
